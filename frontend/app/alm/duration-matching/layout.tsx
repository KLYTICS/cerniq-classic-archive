import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — duration matching' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
