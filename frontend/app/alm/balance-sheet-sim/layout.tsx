import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — ualance sheet sim' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
