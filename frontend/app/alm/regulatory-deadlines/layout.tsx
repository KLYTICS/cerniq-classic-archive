import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — regulatory deadlines' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
