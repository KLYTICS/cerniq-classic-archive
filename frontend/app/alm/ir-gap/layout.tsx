import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — ir gap' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
