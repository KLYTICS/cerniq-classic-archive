import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — swap valuation' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
