import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — deposit runoff' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
