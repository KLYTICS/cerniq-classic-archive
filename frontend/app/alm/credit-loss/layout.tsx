import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — credit loss' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
