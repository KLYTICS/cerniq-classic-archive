import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — capital adequacy' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
