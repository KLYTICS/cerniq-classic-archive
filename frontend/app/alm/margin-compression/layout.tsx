import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'CERNIQ ALM — margin compression' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
