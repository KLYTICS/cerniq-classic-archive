- 2026-05-16 — **security(controllers) + verifier(R5): canonical `req.user` identity-extraction chain locked across controllers; 12 audit-flagged divergences normalized to `userId ?? id ?? sub ?? <sentinel>`.** Closes the standing `IDOR_RESIDUAL_AUDIT.md:116` sweep ("userId extraction inconsistency — Normalize the chain repo-wide so all controllers agree on the auth-source field order"). The audit-doc explicitly flagged this as a deferred follow-up because the canonical field is set by `auth.guard.ts:271` (`request.user = { ..., userId: applicationUserId, ... }`), but legacy JWT-strategy paths still surface `req.user.id` (JWT payload `iat`) and `req.user.sub` (JWT subject) — controllers reading `req.user?.id` directly picked up whatever happened to be in the auth payload instead of the canonical app-userId, misattributing work in audit logs and weakening IDOR helpers that compare on the wrong key. **Verifier (`backend-node/scripts/verify-userid-chain.mjs`, +278 lines, NEW):** for every line in `src/**/*.controller.ts` that reads `req.user?.id` or `req.user?.sub`, the same line MUST also contain `req.user?.userId` AND `userId` MUST appear textually BEFORE the first `id`/`sub` access (so the `??` chain falls through in canonical order). Escape hatch is the standard pattern: `// verify:userid-chain-skip — <non-empty reason>` on the same line OR the immediately-preceding line — empty reason does NOT exempt (matches R2/R3 convention). 16-case `--self-test` locks the rule shape (canonical OK / sentinel-OK / id-only VIOLATION / id-or-sub no-userId VIOLATION / property-assignment VIOLATION / wrong-order VIOLATION / userId-alone OK / skip-inline OK / skip-above OK / skip-empty-reason VIOLATION / comment-only OK / two-on-line line-level / two-on-adjacent-lines both / bare-no-optional VIOLATION / sub-alone VIOLATION). Live: **60 controller(s) scanned, 0 violation(s)** under `--strict`. **12 site fixes across 4 files (the violation set R5 caught on first live run):** (1) `src/close/close.controller.ts` — 9 audit-actor sites (`signOff:96`, `reopen:110`, `updateTask:127`, `reviewReconciliation:144`, `runTieOut:160`, `postJournalEntry:181`, `reverseJournalEntry:196`, `runFlux:221`, `listActivity:350`) all read `req.user?.id ?? 'system'`/`?? null` — bug class is "audit actor falls back to `'system'` sentinel because canonical `userId` was never read"; normalized each to `req.user?.userId ?? req.user?.id ?? req.user?.sub ?? <sentinel>`, AuthedRequest interface widened from `{ id: string }` → `{ userId?: string; id?: string; sub?: string }` with rationale comment naming the AuthGuard canonical-field source. (2) `src/actions/action.controller.ts:62` — `userId: req.user?.id` in action-dispatch metadata object; widened inline `@Req()` type to canonical 3-field shape + replaced read with `userId: req.user?.userId ?? req.user?.id ?? req.user?.sub`. (3) `src/ai-advisor/ai-advisor.controller.ts:97` (`listSessions`) — read `req.user?.id ?? req.user?.sub ?? 'anonymous'`, was the same audit-doc-flagged regression that `ask()` already fixed; normalized to `userId ?? id ?? sub ?? 'anonymous'`. (4) `src/alm/exam-prep/camel-certification.controller.ts:212` (`certify`) — `req.user?.id || req.user?.sub` (using `||` truthy-fallback instead of `??` nullish); normalized to canonical `??` chain leading with `userId`. **Wired into `npm run lint`:** new `verify:userid-chain` script in `backend-node/package.json` (between `verify:body-trust` and `verify:rule-4-audit-immutable`), so future regressions fail CI. **Trio invariants on commit:** verify-userid-chain 60/0, verify-body-trust 64/510/36/0 strict, verify-auth-coverage 60/510/453/46/0 strict, verify-institution-scope-guard 132/132+12/12+11/11+1/1/0; ai-advisor.controller.spec.ts (10/10), close.service.spec.ts + action-registry.service.spec.ts + camel-scorer.service.spec.ts + camel-forecaster.service.spec.ts (46/46) all green post-fix. **D24 ratchet pattern §1–4 satisfied:** floor (12 violations) measured before lock; verifier locked at 0; ratchets only direction is "must remain 0" (can only TIGHTEN by adding new detection forms); `--self-test` embedded in script. **Cross-session coordination:** lane claimed as `cerniq:R5-userid-chain-ratchet`; explicit-path `git commit --only` scoped to 6 files (`package.json` + verifier + 4 controllers); peer enterprise-audit-swarm's concurrent Rule 11 rationale work on `alm.controller.ts` and the new paired `alm.controller.security.spec.ts` deliberately untouched (different lane, their staged `2026-05-16-r3-v3-closure.md` handoff-incoming file is theirs to land); peer cold-storage-triage's `docs/ops/cold_storage_revival.md` + `scripts/pre-deploy-smoke.sh` untouched (their lane). — `backend-node/scripts/verify-userid-chain.mjs`, `backend-node/package.json`, `backend-node/src/close/close.controller.ts`, `backend-node/src/actions/action.controller.ts`, `backend-node/src/ai-advisor/ai-advisor.controller.ts`, `backend-node/src/alm/exam-prep/camel-certification.controller.ts`
