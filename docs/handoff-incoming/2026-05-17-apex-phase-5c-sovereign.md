- 2026-05-17 — **Apex absorption — Phase 5c (/apex/sovereign console with mocked data).** Third of four flagship-route ports. Verbatim port of apex/app/sovereign/page.tsx (381 lines) with two adjustments: (1) `requireSovereignPageAccess()` auth gate stripped (Phase 4 reattaches); (2) `generateSovereignSignals()` + `listThemePromotionStates()` server fetches replaced with mocked payloads shaped byte-for-byte like production contracts. Added `frontend/lib/apex/sovereign-contracts.ts` (119 lines) — full type port: ThemeId enum (5 variants), ThemeTier, ThemeConstituent, ThemeDefinition, TensionRegime (4 variants), TensionComponent, SovereignRegime, SignalSide, SignalComponentScore, ExecutionLane (3 variants), SovereignSignal, ThemePromotionState, SovereignSignalSet. ApexMode inlined as `"DEMO" | "PAPER" | "LIVE"` rather than dragging the full apex/lib/config module. Added `frontend/lib/apex/sovereign-themes.ts` (~485 lines after prettier; 182 source) — verbatim port of the curated 5-theme universe catalog: AI Supply Chain (15 constituents incl. NVDA/AVGO/AMD/TSM/ASML), Tech Broad (13 constituents incl. AAPL/MSFT/GOOGL/META/AMZN), Vanguard Core (8 constituents anchored on VOO/VTI), Quantum Computing (9 constituents incl. IONQ/RGTI/QBTS), Nuclear & Uranium (11 constituents anchored on URA/URNM incl. SMR/OKLO/CEG). Constituent tier weights (anchor:3, core:2, satellite:1) and `getThemeUsRevenueShare()` weighted-average helper preserved verbatim. Added `frontend/app/apex/sovereign/page.tsx` (~700 lines after prettier) — 4-quadrant Bloomberg layout: RegimePanel (tension index 42 / elevated / USD-pressure with 6 components: VIX term, credit spreads, DXY momentum, cross-asset corr, FX vol, funding stress) + ThemeCardsPanel (5 theme cards with promotion lane badges) + SignalStreamPanel (8 mocked signals sorted by |score|×conf: NVDA/MSFT/VOO live, URA/TSM short/META live/CEG paper, IONQ shadow) + PromotionPanel (3 themes live, 1 paper, 1 shadow). COL color tokens preserved verbatim. Note: rendered inside ApexPageShell rather than full-viewport main (cerniq page chrome compatibility). Verification scoped: `eslint app/apex components/apex lib/apex` clean; `tsc --noEmit` reports 0 errors in apex/ paths; `prettier --check` clean on all 3 new files.
