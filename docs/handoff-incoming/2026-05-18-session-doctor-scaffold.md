- 2026-05-18 — **feat(session/doctor): boot-time diagnostic for Claude Code sessions — composes git+peers+CI+disk+worktree+handoff state into one command so new sessions don't burn 10+ tool calls re-discovering the room.** Closes the orientation-tax problem observed across 4–5 parallel Claude sessions on `claude/enterprise-quality-hardening` during the 2026-05-17/18 cleanup arc (squash + CI fix + gitleaks + merge resolution). Pattern: every session opening on cerniq re-runs the same recon sequence (`git status`, `git log`, `claude-peers status`, `claude-peers inbox`, `gh pr checks`, `df`, `git worktree list`) and burns ~30 min before doing real work. The doctor packages it into a single deterministic command with three audiences: humans (color markdown report to stdout), Claude sessions (`--json` for piping into agent prompts), and CI/hooks (`--quiet` exit-code-only for composition). **Severity policy:** CRITICAL exits 2 (real problem — `.git/index.lock` stale, disk <500 MiB on `/private/tmp` which is where the Claude Code tool harness writes per-task output files — observed in this session's ENOSPC SHA-mirage bug), WARNING exits 1 (advisory — behind origin, unread peer msgs, stale worktree WIP >7d, CI failures on current PR, pending squash entries, working-tree changes that may be peer territory), INFO exits 0 (clean observations — disk free, no locks, 0 active peers, inbox empty, recent §5 landings count, HEAD subject). **Checks implemented (10):** `disk_pressure` (BSD `df -k /private/tmp`, <500 MiB = critical), `git_index_lock` (with `lsof` to distinguish live-git from macOS Spotlight `mdworker` residue per `[[feedback_macos_spotlight_git_lock]]`), `branch_position` (ahead/behind + actionable git command), `working_tree` (M/??/S breakdown via `git status --porcelain=v1`, NOT trimmed because trim() strips the leading space from `" M"` lines), `peer_claims` (live count + mine vs other), `peer_inbox` (unread count via `claude-peers inbox --peek`), `recent_landings` (count of `- YYYY-MM-DD —` lines in §5 today + yesterday), `handoff_incoming_pending` (entries awaiting squash), `stale_worktrees` (worktrees >7d old with >5 WIP files — flags the heuristic-roentgen/cool-shamir/xenodochial-williamson agent-worktree residue), `ci_pr_state` (gh pr checks rollup, wraps `|| true` since `gh pr checks` exits non-zero on any failure which would have been swallowed by `safe()`). **Embedded `--self-test`** (D24 ratchet convention — 9 cases covering severity ordering, BSD `df` parse, exit-code mapping, severity reducer, color noop). **Wired:** `npm run session:doctor` (human), `npm run session:doctor -- --json` (Claude/agent), `npm run session:doctor -- --quiet` (CI), `npm run session:doctor:self-test`. **Not wired into `verify:local:critical`** — doctor is informational, not gating; making it block on warnings would over-fire (CI failures on a11y-sweep are pre-existing infra). **Style:** matches `scripts/swarm/doctor.mjs` check-runner pattern + `scripts/cerniq-status.mjs` color-helper style. Self-contained (doesn't import `_lib.mjs` so it runs in stripped trees). Zero runtime deps. **Bug surfaced + fixed during build:** `safe()` and `git()` helpers call `.trim()` on output, which strips the leading space from porcelain-v1 status lines like `" M package.json"`, breaking the unstaged-vs-staged classification (showed `0M 1S` instead of `1M 0S`). Fix: `working_tree` check runs `execSync` directly without trim. **Inspired by the session this commit was authored in:** during a 4-hour cleanup arc the assistant repeatedly ran the same ~15 tool calls just to recover situational awareness; the doctor codifies that recovery sequence so the next session starts hot. — `scripts/session/doctor.mjs`, `package.json`
