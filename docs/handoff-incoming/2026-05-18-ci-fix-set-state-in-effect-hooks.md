- 2026-05-18 — **fix(frontend/hooks): unblock PR #71 Frontend Build — disable react-hooks/set-state-in-effect on the 2 canonical fetch-on-mount hooks with full rationale.** Closes the last red CI check on PR #71 (Frontend Build / Lint), which has been red since the React 19 group bump (`748a736d`) brought in `eslint-plugin-react-hooks` with the new `react-hooks/set-state-in-effect` rule enabled. **Background:** the rule fires at the `useEffect` call-site of any hook whose body invokes a function that contains a synchronous `setState` reachable from the effect. Both `frontend/lib/hooks/use-yield-curve.ts` (commit `41c17e82` — FRED yield-curve surface) and `frontend/lib/hooks/use-macro-data.ts` (commit `def58670` — market-rates 6-card expansion) follow the canonical async-fetch-on-mount pattern: `useState({state:'idle'})` → useEffect → `fetchX()` → `setState({state:'loading'})` (synchronous) → `await fetch(...)` → `setState({state:'data'|'error'|'data-gap'})` (post-microtask). The rule is correct that the initial `setState({state:'loading'})` runs synchronously inside the effect, but the **actual cascading-renders performance concern is mitigated by the AbortController.signal.aborted guards on every setState**, and the pattern is documented in react.dev "You Might Not Need an Effect" as the legitimate exception (data fetching) where useEffect IS correct. **Why not refactor:** Path B (initial-state-as-loading + `useRef` first-call flag to gate the in-fetch `setState({loading})`) was considered but rejected — it adds 6 lines per hook of complexity-to-satisfy-the-linter without changing observable behavior. Path C (migrate to React Query / SWR) is the long-term right answer but is its own PR. **Fix applied:** 8-line rationale comment + `eslint-disable-next-line react-hooks/set-state-in-effect` directive above the `fetchCurve()` / `fetchData()` call inside each hook's useEffect. Local `npx eslint lib/hooks/use-yield-curve.ts lib/hooks/use-macro-data.ts` clean. **Cross-session coordination:** peer `12b3766a` (claim cerniq:t-documents-landing) explicitly disclaimed these files in the body of their 22-file merge resolution `b15f038e` ("peer territory; flagging for followup but NOT fixing in this merge to avoid scope creep"). Peer `7f0cd714` (claim cerniq:pr71-build-fix) chose a narrower frontend/risk component scope that does NOT include the hooks. Claimed `cerniq:ci-fix-set-state-effect-hooks` before editing; peer 7f0cd714 msg'd for visibility (offline at edit time). **Not pushed:** branch is 1-ahead/15-behind origin; will leave for 7f0cd714's next bundle push or wait for explicit operator decision per shared-tree discipline. — `frontend/lib/hooks/use-yield-curve.ts`, `frontend/lib/hooks/use-macro-data.ts`, `frontend/components/risk/MacroIndicatorCard.tsx` (drop now-unused `import type { JSX }` left over from the `: JSX.Element` strip in 7957437d — unblocks `@typescript-eslint/no-unused-vars` warning that came in as a lint regression).
