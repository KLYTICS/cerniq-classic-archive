- 2026-05-17 — **feat(vendor): Tier-1 ECB SDW + Alpha Vantage providers — EUR macro + intraday US fallback.** Fourth commit in the vendor-connectivity arc (after `41c17e82` FRED → `def58670` multi-card → `db410efe` framework). Promotes two `planned`-status vendors to `production` with full provider implementations + 32/32 spec coverage. **EcbSdwProvider** (`backend-node/src/market-data/providers/ecb-sdw.provider.ts`, ~296 LOC + 192-line spec / 16 tests): ECB Statistical Data Warehouse — public, no auth. CSV format selected over SDMX-JSON for ~50× smaller payloads (CSV trades dimension metadata for raw observation rows). Methods: `getLatestObservation(dataflow, seriesKey, { lookbackDays })` walks past blank-OBS_VALUE trailing rows (ECB common pattern at month boundaries); `getYieldCurve()` builds EUR_AAA_SOVEREIGN curve from 10 tenors (3M/6M/1Y/2Y/3Y/5Y/7Y/10Y/20Y/30Y) via Promise.allSettled — partial failures don't kill the whole curve; inversion detection on 10Y−2Y spread; `getHICP()` wraps the harmonized inflation series as EconomicIndicatorDto; `getECBReferenceRate(ccy)` returns EUR-base FX (`D.<CCY>.EUR.SP00.A`) with ISO-3-letter guard; `getKeyRate(key)` labels DFR/MRO/MLF correctly from the well-known SDW key suffix. Defensive parsing: header-driven column indexing (TIME_PERIOD + OBS_VALUE positions), null on missing columns, null on every-row-unparseable, null on 404 or network error. **AlphaVantageProvider** (`backend-node/src/market-data/providers/alpha-vantage.provider.ts`, ~294 LOC + 195-line spec / 16 tests): free-tier US-focused market data via REST. Reads `ALPHA_VANTAGE_API_KEY` at call time (test-mutable env). Methods: `getQuote(symbol)` via GLOBAL_QUOTE function (most rate-limit-friendly — one observation per call vs. ~100 bars for TIME_SERIES); `getFXRate(from, to)` via CURRENCY_EXCHANGE_RATE; `getRSI(symbol, interval, timePeriod)` returns most-recent RSI value. **Critical discipline detail:** Alpha Vantage emits HTTP-200 responses for rate-limit refusals via three sentinel keys — `Note` (rate-limit), `Information` (premium-required endpoint), `Error Message` (invalid symbol). All three are detected in `isRateLimitedOrError()` with logged warn + null return — Rule 1 honest-gap behavior. Symbol regex `/^[A-Z0-9.\-]{1,15}$/` rejects malformed input without fetching (defensive — no upstream credit burned on bad input). ISO-3-letter currency code validation, interval allow-list, integer-only timePeriod in [2, 200] for RSI. **Multi-provider posture:** at 25 req/day per IP, Alpha Vantage is a **fresh-quote / intraday fallback**, NOT a primary feed. Cerniq's circuit-breaker + cache (`MarketDataService.fetchWithTelemetry`) keeps usage well under the daily ceiling. **Module wiring** (`market-data.module.ts`): EcbSdwProvider + AlphaVantageProvider added to providers array; not exported as the consumers are MarketDataService internal failover hops. **Registry updates** (`backend-node/src/vendor/registry.ts`): alpha-vantage status `planned` → `production`, ecb-sdw status `planned` → `production`. Both gain `providerPath` pointing to their concrete provider files. `blockedBy` removed (no longer blocked); descriptions tightened to reflect actual capability surface; PR-cooperativa relevance copy strengthened with concrete use-cases. Admin page at `/admin/vendor-status` auto-reflects new statuses via registry read — no frontend code change needed. **KLYTICS discipline (all 4 verifiers GREEN at commit time):** Rule 1 — every method returns `T | null`, every error path logs warn before null; no silent zeros, no fabricated defaults. Rule 4 — no audit_log mutations. Rule 7 — every DTO carries `provider: 'ecb-sdw' | 'alpha-vantage'` lineage breadcrumb plus seriesKey/symbol context. Rule 11 — `// type-rationale:` on 4 `error: unknown` catch-block narrowings; baseline unchanged. Rule 12 — no randomness. **Test coverage:** 32/32 new (16/16 ECB SDW + 16/16 Alpha Vantage); cumulative `src/market-data/providers` suite stays green. **Vendor framework status post-commit:** 17 vendors total — 4 production (Yahoo, CoinGecko, FRED, Treasury Fiscal Data) + 2 production NEW (ECB SDW, Alpha Vantage) + 1 production filings (SEC EDGAR) + 6 scaffold (Bloomberg/Refinitiv/ICE/Symitar/COSSEC/NCUA 5300) + 4 planned (Polygon, KeyStone, Verafin, LexisNexis). Production count: 7/17 (41%). **What this does NOT include:** real Refinitiv/Bloomberg/ICE BofA documented-API scaffolds upgraded from stub to real-call-when-creds (next in Tier-2 expansion); real Symitar/COSSEC/NCUA (contracts + sandbox creds). — `backend-node/src/market-data/providers/ecb-sdw.provider.ts`, `backend-node/src/market-data/providers/ecb-sdw.provider.spec.ts`, `backend-node/src/market-data/providers/alpha-vantage.provider.ts`, `backend-node/src/market-data/providers/alpha-vantage.provider.spec.ts`, `backend-node/src/market-data/market-data.module.ts`, `backend-node/src/vendor/registry.ts`
