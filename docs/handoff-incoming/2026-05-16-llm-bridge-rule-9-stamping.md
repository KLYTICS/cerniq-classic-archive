- 2026-05-16 — **feat(llm-bridge): KLYTICS Rule 9 prompt + cost provenance on agent-runner core LLM bridge — shrinks Rule 9 baseline 3 → 2; cascade-closes 3 pre-existing tsc errors.** Fifth Rule 9 chip-away of the session (after `d1d42e97` analyst, `c6605953` advisor-v2, `9f5c6677` ai-advisor, `6892d7e6` impact-extractor) and the largest-blast-radius one: `agents/runner/llm-bridge.service.ts` is the single Anthropic SDK call site every agent run flows through. The baseline note for this file explicitly directed *"stamp once `LLMTurnResponse` shape extended with `promptVersion` + `usage` fields (cascades to all agents)"* — done as directed. **Structural difference from prior 4 chip-aways:** this service is a *primitive* — the caller (agent-runner) supplies `req.system`, `req.tools`, and `req.temperature` per call. So `computePromptVersion` must run inside `turn()` per invocation, not at module-init like the analyst/advisor surfaces. The fingerprint reflects the resolved temperature (the value the SDK actually saw, not just the explicitly-passed one) — `DEFAULT_TEMPERATURE = 0.2` promoted to a named module constant so the fingerprint AND the SDK call read the same source (previously the literal `0.2` was inline at `temperature: req.temperature ?? 0.2`, drift-prone between the two). **What changed:** (1) `llm-bridge.service.ts`: widened `LLMTurnResponse` with `promptVersion: string` + `usage: LLMUsage | null` (additive — existing `inputTokens`/`outputTokens` fields retained so `agent-runner.service.ts:210-211` 2-class cost-circuit-breaker path is untouched); `turn()` computes `promptVersion` before SDK call, extracts 4-class `usage` after, emits a structured `logger.log({ event: 'rule-9-stamp', surface: 'agents.llm-bridge.turn', model, promptVersion, usage, costCents, pricingVersion, costMissingReason, latencyMs, stopReason, toolCallCount, temperature })` entry. `costMissingReason` is a 3-way discriminator — `null` (priced ok), `'NO_USAGE_IN_RESPONSE'` (SDK omitted usage block, e.g. error path), or `'NO_PRICING_DATA'` (model not in PRICING table) — making the Rule 1 null-cost discipline auditable per-call. (2) `llm-usage.ts`: added `claude-opus-4-6` entry to PRICING table (input $15.00, output $75.00, cache_creation $18.75 = 1.25× input, cache_read $1.50 = 0.10× input — uniform Anthropic family ratios per the existing Sonnet entry); bumped `LLM_PRICING_VERSION` `2026-05-15 → 2026-05-16` so a future query for "which audit rows were costed under what pricing snapshot" doesn't confuse a Sonnet row from before the Opus entry was added with one from after. (3) **Cascade fix in same file**: widened `AnthropicResponseUsage` interface to accept `number | null | undefined` on all four token classes (was `number | undefined`). **`tsc --noEmit` found 4 errors at the start of this PR — 1 from my new code, 3 PRE-EXISTING in peer-owned files at `ai-advisor.service.ts:229`, `ai-advisor.service.ts:432`, `impact-extractor.service.ts:57`** that landed in `9f5c6677` and `6892d7e6` earlier today. Root cause was the helper's type lying about the SDK shape: Anthropic's `Message.usage.cache_creation_input_tokens` is `number | null`, not `number | undefined`. The `?? 0` defaults inside `extractUsage` already collapse `null` and `undefined` identically — runtime was always correct; only the type signature was wrong. Widening the type closes all 4 errors at the helper, with zero behavior change and zero edits to peer-owned source files. **Why these accumulated without blocking earlier:** husky pre-commit only runs `backend tsc incremental` as **warn-only**; full `tsc --noEmit` lives inside `npm run lint` (CI). Same shape as the [[feedback_drift_only_ci_gates]] pattern — a CI gate at the wrong layer lets type errors compound across peer commits silently. (4) `verify-rule-9-stamping.mjs`: removed `agents/runner/llm-bridge.service.ts` from `BASELINE_UNSTAMPED`; fixed stale self-test fixture that still pointed at `ai-advisor/ai-advisor.service.ts` (which was removed from baseline in `9f5c6677` without updating the test — caught by `--self-test` pre-edit, since `--self-test` isn't in `npm run lint`, the staleness had slipped through). (5) `llm-usage.spec.ts`: added regression-lock test `'collapses null per-field usage to 0 (SDK emits null, not undefined)'` — locks the contract so a future type-narrowing of `AnthropicResponseUsage` breaks loudly. **Verifier closure:** `verify-rule-9-stamping` now reports `5 stamped · 0 skip-commented · 2 baselined · 0 unstamped` (vs. `4·0·3·0` pre-PR; vs. `1·0·6·0` at session start — **67% baseline reduction in this single session**). Remaining 2: `alm-advisor.service.ts` (v1 legacy — retire-or-stamp decision pending), `nl-ingest.service.ts` (NL ingest pipeline; stamp blocked on analysis-run row lineage wiring). **Test coverage:** jest `20/20` (llm-usage 7→8 with new null-collapse case, llm-bridge.cost.spec untouched at 8); `verify-rule-9-stamping --self-test` `11/11`; `npx tsc --noEmit` clean (vs 4 errors pre-PR). Live verifier passes. **Note: no spec on `turn()`** — the existing `llm-bridge.cost.spec.ts` only tests static `computeCostUsdCents`. The dynamic Anthropic SDK client makes jest-mocking impractical (same constraint that left advisor-v2/ai-advisor/impact-extractor's LLM paths without spec coverage); the verifier is the substitute discipline. Adding a `turn()` spec would require mocking `@anthropic-ai/sdk`'s `messages.create` against a stable usage-block fixture — separate work, not blocking. **Maturity matrix delta:** cerniq Rule 9 cell stays 🟡 (2 services still baselined) but the chip-away ratchet moves correctly per canon §3 footnote. The "cascades to all agents" payoff from the baseline note: every agent run now emits a structured stamp readable by audit queries on `event: 'rule-9-stamp'`; downstream consumers (agent-runs.controller aggregation) can optionally start reading `turn.usage.cacheReadInputTokens` etc. for richer cost attribution — kept out of this PR's blast radius. **Cross-session protocol:** uses the new `docs/handoff-incoming/` landing-gate pattern (zero SESSION_HANDOFF.md stage-race surface — 4+ peer-collision absorptions happened in this session before the structural fix landed). Claimed lane `cerniq:cerniq:rule-9-llm-bridge-stamping` with explicit paths; `git commit --only` scopes the commit to the 5 paths and excludes any concurrent peer activity. — `backend-node/src/agents/runner/llm-bridge.service.ts`, `backend-node/src/alm/analyst/llm-usage.ts`, `backend-node/src/alm/analyst/llm-usage.spec.ts`, `backend-node/scripts/verify-rule-9-stamping.mjs`, `docs/handoff-incoming/2026-05-16-llm-bridge-rule-9-stamping.md`
