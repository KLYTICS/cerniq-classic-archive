- 2026-05-17 — **Apex absorption — Phase 5d (/apex/war-room lobby with mocked session list).** Fourth and final flagship-route port under Phase 5. The original apex/app/war-room/page.tsx (503 lines) is the heaviest absorbed surface — depends on 4 heavy panels (BetweenSessionContinuityCard, OvernightOperationsPanel, RuntimeHealthStrip, ShiftStartConsole), 5 hooks (useOperatorHub, useRuntimeSafety, useShiftStart, useApexStore, useWarRoomOperator), live API calls to `/api/war-room/sessions`, and a React Error Boundary for panel-level degradation. Phase 5d ports the *visual identity* of the lobby. Added `frontend/lib/apex/war-room-contracts.ts` (44 lines) — narrowed WarRoomSessionSummary + 4 enum dependencies (WarRoomSessionStatus, WarRoomRoute, WarRoomContinuityState, WarRoomOperatorRole) + DemoOperator view-model type (in apex this comes from `useWarRoomOperator()` hook). Added `frontend/app/apex/war-room/page.tsx` (~520 lines after prettier) — ApexHero with operator MetricStrip (mocked owner-role operator with terminal label/id), ApexRouteGrid (hub/research/journal navigation), ApexSection placeholder for the 4 heavy panels (Phase 6 reattach), ApexStatePanel collaboration-state callout, disabled create-session form ("CREATE SESSION · READ-ONLY DEMO"), active-sessions list with 3 mocked WarRoomSessionSummary entries spanning the continuity-state rainbow (active_driver EUR/USD London-open / observer_only GBP/JPY US-close / driver_stale_resume_required AUD/USD APAC-handoff stale 42m). SessionRow inline-renders each entry with the same color-coded continuity-state pill + 4-column metric grid (pair/route/open-trades/last-activity) + driver attribution + recommended-action button (disabled, " · DEMO" suffix). `formatAge()` helper preserved verbatim from apex source. Note: Apex's React Error Boundary recovery view (used when a heavy panel fails at runtime) is omitted from this static port — Phase 6 reintroduces it alongside live panels that could actually fail. Verification scoped: `eslint app/apex components/apex lib/apex` clean; `tsc --noEmit` reports 0 errors in apex/ paths; `prettier --check` clean on 2 new files. Phase 5 complete (4/4 routes): journal/cockpit/sovereign/war-room all ported as display-only surfaces.
