- 2026-05-16 — **feat(ai-advisor): KLYTICS Rule 9 prompt + cost provenance on both call paths — shrinks Rule 9 baseline 5 → 4.** Second chip-away of the session (after `c6605953` advisor-v2), and the largest remaining LLM-calling surface in cerniq: `ai-advisor.service.ts` has two distinct Anthropic call sites — `ask()` uses `messages.create` in a tool-use loop (≤5 rounds), `streamAsk()` uses `messages.stream` with `stream.finalMessage()` for the message_stop usage payload. Both now reuse the canonical `computePromptVersion` + `extractUsage` + `mergeUsage` + `estimateCostCents` helpers from `src/alm/analyst/` (third consumer of the shared module). **What changed:** (1) Promoted `AI_ADVISOR_TEMPERATURE = 0.3` to a named constant so the temperature participates in the prompt fingerprint AND the SDK call from the same source — previously the magic number was inline and could drift between fingerprint vs call. (2) `ask()` path: `promptVersion` computed once before the tool-use loop with `(model, systemPrompt, tools: ADVISOR_TOOLS, temperature)` tuple; four-class `usage` accumulator (`LLMUsage | null`) introduced **alongside** the existing 2-class `totalInputTokens` + `totalOutputTokens` roll-up — deliberate parallel tracking so the DB-persisted `tokenCount` field (read by `conversation_history`) doesn't change schema while the four-class stamp emits for audit. Each loop iteration: `usage = mergeUsage(usage, extractUsage(response))`. After the loop settles, `estimateCostCents(AI_ADVISOR_MODEL, usage)` runs and a structured logger entry fires with the full stamp (`event: 'rule-9-stamp', surface: 'ai-advisor.ask', model, promptVersion, usage, costCents, latencyMs, toolRounds, pricingVersion | costMissingReason`). (3) `streamAsk()` path: same promptVersion computation, but usage is captured in the `message_stop` event handler via `extractUsage(finalMsg)` (the existing `await stream.finalMessage()` already runs there — clean reuse). Same structured-logger stamp shape with `surface: 'ai-advisor.streamAsk'` so a log query on `event: 'rule-9-stamp'` returns both surfaces correlated by `(institutionId, sessionId)`. (4) **Rule 1 compounds** — if usage is null (e.g. stream errored before message_stop), `costCents: null` with `costMissingReason: null`; if the model isn't in the pricing table, `costMissingReason: 'NO_PRICING_DATA'`. Never silent-zero. **Verifier closure:** `scripts/verify-rule-9-stamping.mjs` `BASELINE_UNSTAMPED` entry for `ai-advisor/ai-advisor.service.ts` removed (the note read "blocked on peer IDOR-fix claim (sid=8f694e66); promote to stamped once that wave settles" — that wave has settled, `b2a64c25` + `9dbf57df` landed, the claim is released). Verifier now reports `3 stamped · 0 skip-commented · 4 baselined · 0 unstamped` (vs. `2 stamped · 5 baselined` pre-PR, vs. `1 stamped · 6 baselined` at start of session). Remaining baseline: `llm-bridge` (agent-runner core), `alm-advisor` (v1 legacy), `nl-ingest`, `impact-extractor` — each retains its one-line follow-up direction. **Landing-gate adoption:** this is the **first commit that uses the new `docs/handoff-incoming/` pattern** instead of the direct SESSION_HANDOFF.md §5 insert. Reason: a peer wave running the structural-fix to the landing-gate hook landed concurrently and rewrote SESSION_HANDOFF.md, erasing my awk-inserted bullet (the 4th SESSION_HANDOFF stage-race of the session). The incoming-pattern's per-commit unique-named file has zero contention surface — exactly the failure mode it was designed for. Squash via `node scripts/squash-handoff-incoming.mjs` will merge this into §5 at the next session boundary. **Test coverage:** `ai-advisor.service.spec.ts` 11/11 green in 0.7s with zero spec modifications — additive changes only (new accumulator + new log call, no shape changes to existing SDK call args beyond `temperature: 0.3` → `temperature: AI_ADVISOR_TEMPERATURE` which is the same value). **Why structured logger (not SSE event):** mirrors the advisor-v2 rationale — `streamAsk()` yields `AsyncGenerator<AiAdvisorChunk>` where `AiAdvisorChunk = { type: 'text' | 'tool_use' | 'done', ... }`; broadening the type to carry the stamp would cascade to all WebSocket consumers. The canon's "observable for audit" requirement is satisfied via structured log; SSE-visible cost is a separate UX choice the product can make later. **Maturity matrix delta:** cerniq Rule 9 cell stays 🟡 (4 services still in baseline, 3 stamped) but the chip-away ratchet moves correctly — the `verify-rule-9` stamped/baselined counts are the measured metric the canon §3 footnote tracks. — `backend-node/src/ai-advisor/ai-advisor.service.ts`, `backend-node/scripts/verify-rule-9-stamping.mjs`, `docs/handoff-incoming/2026-05-16-ai-advisor-rule-9-stamping.md`
