- 2026-05-16 — **Cold-storage revival runbook — round-2 audit.** Follow-up to `a69e44fa`. Round-1 fixed §4 Cloudflare→Spaceship in isolation; round-2 audits the rest of the doc for the same kind of drift and catches 6 additional bugs: (1) §0 table + §3 wrongly claimed Vercel deleted — `capexcycle` survived cold-storage with both `.vercel/project.json` files intact; new §3.0 "is Vercel actually deleted?" gate added, §3.1 reworked to use the surviving project name; (2) project name `cerniq-frontend` → `capexcycle` in §3.4, §8 hand-back, §9 rollback; (3) §9 rollback reframed `vercel projects rm` from instruction to anti-pattern (destroying the surviving project removes the cached-edge fallback); (4) §5.2 Resend DNS reference: Cloudflare → Spaceship; (5) §8 hand-back peers-msg: `claude-peers msg cerniq` was ill-formed (CLI accepts `<sid-prefix|all>` only), replaced with `msg all` + `handoff --project cerniq`; (6) Audience line + Appendix C cost table: Cloudflare scoped to "optional R2+WAF only", Spaceship line added to cost table. Appendix B gains a Round-2-caught-what-round-1-missed lesson with the meta-rule (grep the whole doc for the same noun before declaring a factual-drift fix complete). Smoke `--self-test` also cleaned: removed the hollow `1=1` exit-mapping assertion, now 10/10 real assertions. Verification: `scripts/pre-deploy-smoke.sh` still 42/44 baseline, `--self-test` 10/10 PASS, secret-shape scan clean, no remaining Cloudflare-as-DNS or `cerniq-frontend` references outside historical-context retrospectives. SHA `1d57f774` on `claude/enterprise-quality-hardening`. Receipt for [[inter-session-protocol]] Rule 10 and [[sustained-autonomy-self-observations]] diagnosis-without-fix-audit bias caught in my own prior work.
