- 2026-05-16 — **deploy: pre-deploy smoke gate flipped READY (43/44 PASS) + Vercel frontend deploying live; backend api.cerniq.io remains BLOCKED on operator-only Railway revival.** Investigation triggered by P0 production finding (`api.cerniq.io` SSL cert mismatch — served `*.up.railway.app` wildcard, no cert for the custom domain). Root cause: Railway project deleted in cold-storage pivot 2026-05-09; DNS still points at deleted endpoint `48l0ranw.up.railway.app`. Frontend at `cerniq.io` returns 200 OK on all 5 sampled public routes (cerniq.io, /login, /pricing, /get-started, /dashboard) but the API endpoint browser calls would hit cert warnings. **What I did this turn (code-side only — dashboard work is operator-only per runbook):** (1) Ran `bash scripts/pre-deploy-smoke.sh` — failed 1/44: "Backend TypeScript compiles". (2) Located the error at `backend-node/src/market-data/market-data.service.spec.ts:50` — `MarketDataService` constructor signature was extended with `FredProvider` (4th param) but the spec was only passing 3 mocks. A peer landed the fix concurrently with my Edit attempt (added `mockFredProvider` with 5 method mocks all returning null, then included it in the constructor call between `mockCoinGeckoProvider` and `mockDataQualityService`) — verified `npx tsc --noEmit` clean. (3) Re-ran smoke: **43 PASS · 0 FAIL · 1 advisory WARN** (warning: working tree not clean — peer activity, unrelated). **PRE-DEPLOY GATE: READY**. (4) Vercel project `capexcycle` (id `prj_odl6Ltja3NXGwJI0v7jZ7NEs88bL`) confirmed alive — production deployment `capexcycle-jmj5ep7m7-ekiess-projects.vercel.app` Building at the moment of this entry (~4 min in; prior deploys averaged 2m). Recent successful prod deploy was `capexcycle-an2ng0abq` 30 min ago — Vercel auto-deploy pipeline IS healthy. (5) Confirmed prod env vars correctly configured: `NEXT_PUBLIC_API_URL=https://api.cerniq.io`, `NEXT_PUBLIC_NODE_API_URL=https://api.cerniq.io`, `NEXT_PUBLIC_APP_URL=https://cerniq.io` — frontend points where it should; the broken layer is *only* the backend service at the target hostname. **What remains (operator-only, per `docs/ops/cold_storage_revival.md`):** §1 dump-vs-fresh decision → §2 Railway revival (`railway login` + `railway init --name cerniq-api` + provision Postgres) → §3 Vercel env-var refresh against the new Railway hostnames (`vercel env add` for `DATABASE_URL`, possibly `INTERNAL_API_URL`) → §4 Spaceship DNS update (the `api` CNAME from `48l0ranw.up.railway.app` to the new Railway hostname from §2.7) + Railway Custom Domain registration to issue the Let's Encrypt cert for `api.cerniq.io` → §5 Stripe + Resend rewiring. Runbook is precise; ~60–90 min if dump recoverable, 2–4h if fresh. **None of §1–§5 is automatable from a CI job** (per runbook line 1) — all require owner-level dashboard auth at Railway / Vercel / Spaceship. I cannot click in those dashboards from a Bash tool. **Health-check verification commands the user can run after revival** (from §4.2): `dig +short CNAME api.cerniq.io @1.1.1.1` (expect new railway hostname), `dig +short TXT _railway-verify.api.cerniq.io @1.1.1.1` (expect Railway-issued verification token), `echo | openssl s_client -servername api.cerniq.io -connect api.cerniq.io:443 2>/dev/null | openssl x509 -noout -subject` (expect `subject=CN=api.cerniq.io` not `*.up.railway.app`), `curl -sSI https://api.cerniq.io/health | grep -i 'x-railway-fallback'` (expect EMPTY — presence means the new service is missing too). **Cross-session protocol:** uses the `docs/handoff-incoming/` pattern (the very pattern shipped in `da34b7ff`) — single new file at unique name, zero contention with the active peer fixes happening on `backend-node/src/realtime/` and elsewhere. — `docs/handoff-incoming/2026-05-16-deploy-gate-green-backend-blocked.md`
