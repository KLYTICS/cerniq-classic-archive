- 2026-05-16 — **Apex absorption — Phase 2 (operator hub with mocked data).** Ported `apex/components/operator-hub-summary-view.tsx` verbatim to `frontend/components/apex/operator-hub-summary.tsx` (244-line port preserving hyperscript pattern, exact color constants, `data-testid` selectors). Added `frontend/lib/apex/operator-hub-contracts.ts` — Phase 2 contract shim with the subset of `OperatorHubFirstPaintSummary` types the view consumes (full Apex contracts.ts reaches into ~12 lib modules; Phase 3 will widen this). Added `frontend/app/apex/hub/page.tsx` rendering `<OperatorHubSummary firstPaint={MOCK_HUB_SNAPSHOT} />` plus `<ApexRouteGrid>` of the seven absorbed sub-surfaces (cockpit/war-room/journal/sovereign/research/platform/community). The mocked snapshot is shaped byte-for-byte like a production `OperatorHubFirstPaintSummary` so Phase 3 only needs to swap the static payload for a Supabase fetch — the view requires zero changes. Verification: `cd frontend && npm run lint` passed end-to-end (eslint clean, verify-alm-registry 99/101/0, verify-no-orphan-tests 86/0 orphans, verify-rule-12-frontend 612 src files / 0 violations, `tsc --noEmit` clean, `prettier --check` clean on all 3 new files).
