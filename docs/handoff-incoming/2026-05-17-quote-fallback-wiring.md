- 2026-05-17 — **fix(market-data): wire Alpha Vantage as Yahoo-quote fallback in MarketDataService — closes orphaned-provider gap from 314dc75e.** Seventh commit in vendor-connectivity arc (after `8574606b` health endpoint). Audit-driven follow-up: `314dc75e` shipped AlphaVantageProvider as a DI-wired bean in `MarketDataModule` but did NOT integrate it into the user-visible quote flow. `MarketDataService.fetchQuoteFromProvider` was still Yahoo-only for non-crypto, so the provider sat orphaned — never consumed by `getQuote()`, `getRealtimeQuote()`, or any downstream surface. This commit closes that gap. **Failover ladder added:** for non-crypto tickers, the path is now `cache → Yahoo Finance → Alpha Vantage → null (or stale cache decided by caller)`. Yahoo stays as primary; Alpha Vantage only fires when Yahoo returns null + logs a `warn`-level message so operators see the fallback. Alpha Vantage's 25 req/day per-IP budget is preserved by the null-gate — successful Yahoo path never burns an Alpha Vantage call. Crypto path (CoinGecko) unchanged. **Constructor signature** (`backend-node/src/market-data/market-data.service.ts`): adds `private readonly alphaVantageProvider: AlphaVantageProvider` as the 5th positional arg, after TreasuryFiscalDataProvider, before DataQualityService. Service-spec fixture updated to include `mockAlphaVantageProvider = { getQuote: jest.fn().mockResolvedValue(null) }` (null default — only specific tests activate the fallback path). **4 new fallback tests in `market-data.service.spec.ts`:** (1) falls back to Alpha Vantage when Yahoo returns null — verifies both providers called in order + parsed quote returned; (2) does NOT call Alpha Vantage when Yahoo succeeds — preserves the daily-budget gate by asserting `mockAlphaVantageProvider.getQuote.not.toHaveBeenCalled()` on the happy path; (3) throws NotFoundException when BOTH fail — preserves the "no silent zero" contract; (4) does NOT touch Alpha Vantage for crypto — verifies CoinGecko path remains independent. **MarketDataModule already wires AlphaVantageProvider as a provider** (`314dc75e`), so no module change required — DI resolves the new constructor dep automatically. **KLYTICS discipline (all 4 verifiers GREEN at commit time):** Rule 1 — null-gating preserved; both-fail still throws NotFound (no fake zero quote returned). Rule 4 — no audit_log mutations. Rule 7 — provider field carries lineage (`'alpha-vantage'` or `'yahoo-finance'` per `ProviderFetchResult.provider`). Rule 11 — baseline unchanged (213/979). Rule 12 — no randomness. **Test coverage:** 63/63 in `market-data.service.spec` (up from 59/59), 405/405 cumulative across the full market-data suite. Type-check clean. **Why this matters:** without this wiring, the prior commit's claim "ladder: cache → FRED → Treasury Fiscal Data → stale → null DataGap" was true for yield curves but the *quote* surface had no Alpha Vantage in the chain. The handoff entries are now consistent with shipped code. **Audit lesson** (recorded for [[discover-fix-spec-triangle]]): adding a DI provider isn't sufficient — verify the orchestrator-layer service actually invokes it. Future framework expansions should include a "consumer side" verification step before claiming integration. — `backend-node/src/market-data/market-data.service.ts`, `backend-node/src/market-data/market-data.service.spec.ts`
