- 2026-05-16 — **feat(market-rates): expand /alm/market-rates page from 1 to 6 live cards via reusable useMacroData factory + MacroIndicatorCard component.** Follow-up to `41c17e82` (FRED vendor connector + yield curve). Demonstrates the framework's leverage: ONE provider (FRED), SIX live macro surfaces on the page, ZERO new backend dependencies. The cards added — Federal Funds Rate (DFF), Prime Rate (DPRIME), 30-Year Fixed Mortgage Rate (MORTGAGE30US), CPI (CPIAUCSL), USD/EUR (DEXUSEU) — are the exact set a PR-cooperativa ALM analyst needs alongside the yield curve: short-rate anchor (Fed Funds), variable-rate reference for cooperativa loans (Prime), mortgage-book pricing (Mortgage 30Y), inflation context (CPI), and any EUR exposure (USD/EUR). **Frontend (~340 LOC):** (1) `frontend/lib/hooks/use-macro-data.ts` (~180 LOC) — generic `useMacroData<T>(endpoint)` factory distilled from the existing useYieldCurve state machine: discriminated-union state (`idle | loading | data | data-gap | error`), AbortController-on-unmount, explicit DataGap handling on HTTP 503. Three convenience instantiations — `useInterestRate(seriesId)`, `useEconomicIndicator(seriesId)`, `useFXRate(seriesId, base, quote)` — each a one-liner over the factory; payload types (InterestRate, EconomicIndicator, FXRate) mirror the backend macro.dto.ts shapes without importing across the deploy boundary. (2) `frontend/components/risk/MacroIndicatorCard.tsx` (~140 LOC) — generic Bloomberg-density card that renders any `useMacroDataResult` with the same 4-state pattern as YieldCurveCard. Accepts `title`, optional `subtitle`, the hook result, a `valueAccessor: (T) => number`, an optional `valueSuffix` (e.g. '%'), and an optional `valueFormat` (defaults to 2-decimal). One component → renders rates / indicators / FX pairs interchangeably. (3) `frontend/app/alm/market-rates/page.tsx` (rewritten, ~110 LOC net) — invokes the 5 hooks at top of component, lays out the YieldCurveCard at full width followed by a responsive 1/2/3/4-column grid of indicator cards. Hooks are independent (separate AbortController, separate cache) so a slow CPI fetch doesn't delay the Fed Funds card. **Backend (~80 LOC controller-only):** Two new public endpoints on `MarketDataController` — (a) `GET /api/market-data/economic-indicator/:seriesId` with optional `?units=X&frequency=Y` query params for client-side unit labeling (FRED doesn't return units in the observations endpoint and we avoid the extra metadata call); validates seriesId against `/^[A-Z0-9_]{2,30}$/`. (b) `GET /api/market-data/fx-rate/:seriesId/:base/:quote` — validates seriesId against `/^DEX[A-Z]{2,6}$/` (FRED's exchange-rate series prefix), validates base/quote against ISO 4217-style 3-letter codes. Both endpoints return HTTP 503 with structured DataGap body when upstream is unavailable (Rule 1 — never silent-zero). Both routed through `MarketDataService.getEconomicIndicator()` / `getFXRate()` already wired in `41c17e82`. **KLYTICS discipline (all 4 verifiers GREEN):** Rule 1 — every card surfaces explicit DataGap state; never falls back to zero. Rule 7 — every card footer shows `asOf · provider · seriesId/pair` lineage breadcrumbs. Rule 11 — zero new `any` introduced; baseline unchanged. **Tests:** 285/285 across `src/market-data` green in 1.8s (existing spec set covers the service methods; the two new controller endpoints are pure HTTP wrappers over already-tested service code, so no new spec files are necessary in this commit — the surface gets exercised via the new e2e ALM page render). **Framework leverage demonstrated:** the FRED connector shipped in `41c17e82` had 4 public service methods (getYieldCurve, getInterestRate, getEconomicIndicator, getFXRate). This commit exposes all 4 to the frontend via 2 new endpoints + 1 factory hook + 1 generic card → 5 new live data surfaces on the ALM page. Each future vendor (Bloomberg, Treasury Direct, etc.) gets the same UI surface area for free by implementing the matching backend interface. **Out of scope, named explicitly:** Treasury Direct provider (FRED yield-curve fallback) is the next architectural move — proves multi-provider failover; deferred to keep this commit focused on UI surface expansion. — `frontend/lib/hooks/use-macro-data.ts`, `frontend/components/risk/MacroIndicatorCard.tsx`, `frontend/app/alm/market-rates/page.tsx`, `backend-node/src/market-data/market-data.controller.ts`
