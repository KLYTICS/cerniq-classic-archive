- 2026-05-17 — **fix(frontend/alm): register `/alm/market-rates` in canonical ALM registry.** Closes the `npm run lint` orphan that landed alongside the FRED vendor connector in `41c17e82` and the multi-card expansion in `def58670` — the `app/alm/market-rates/page.tsx` was shipped but the corresponding `AlmModuleSlug` + `ALM_MODULES` entry in `frontend/lib/alm/registry.ts` was missed, so `verify-alm-registry.mjs` (the parity gate wired into `npm run lint`) was failing for every peer touching the frontend. Two-line edit: adds `'market-rates'` to the `rate` block of `AlmModuleSlug` (between `'yield-curve'` and `'svensson'`) and a matching `ALM_MODULES` record (slug `market-rates`, href `/alm/market-rates`, category `rate`, tier `core`, status `ga`, icon `Activity`, bilingual name "Market Rates" / "Tasas de Mercado", description "Live FRED macro: yield curve, Fed Funds, Prime, mortgage, CPI, FX"). After fix: `verify-alm-registry: 100 folder(s), 102 registered, 0 error(s), 0 warning(s)`. Hat tip to peer `2f10ce8d` who spotted the orphan in their `b3600c06` wrap-up and flagged it for whoever picked up next. — `frontend/lib/alm/registry.ts`, `docs/handoff-incoming/2026-05-17-fix-alm-market-rates-orphan.md`
