- 2026-05-16 — **Apex absorption — Phase 5a (/apex/journal display-only port).** First of four flagship-route ports. Added `frontend/lib/apex/trade-journal-contracts.ts` (76 lines) — narrowed contract shim for the panel: `JournalReviewState` enum, `TradeJournalOutcome` enum, `TradeJournalRecord`, `TradeJournalNote`, `TradeThesisSummary`, `TradeJournalReview`, `TradeJournalSummary`. Original `apex/lib/server/overnight-types.ts` reaches into 13 nested types over ~600 lines; Phase 5a narrows to exactly what the panel reads. Added `frontend/components/apex/trade-journal-panel.tsx` (475 lines after prettier) — port of `apex/components/trade-journal-panel.tsx` adapted to the Phase 5 display-only pattern. Original 261-line panel binds directly to `useTradeJournal()` + `useSessionDraft()` (hook duo with API + local-storage state); Phase 5a narrows to a single view-model prop and exposes only internal state for selectedId/pendingState/draft/searchQuery. The Save Journal Review button is disabled and labeled "SAVE JOURNAL REVIEW · READ-ONLY DEMO"; refresh button labeled "READ-ONLY DEMO". Color constants, layout, REVIEW_ACTIONS palette preserved verbatim per absorption directive. Added `frontend/app/apex/journal/page.tsx` (~280 lines after prettier) — mirrors apex's `/journal/page.tsx` layout (ApexHero + ApexMetricStrip + ApexRouteGrid + TradeJournalPanel) with route nav rewritten to `/apex/*`. Six mocked TradeJournalRecord entries span the outcome rainbow (closed_win / closed_loss / executed / blocked / suppressed) and the reviewState rainbow (promoted_learning / thesis_rejected / unreviewed / action_required / execution_anomaly / thesis_confirmed) so designers see every color-coded state render. Verification scoped to Apex files (peer 72c0fb06's market-data WIP still blocks full lint; sent peer-msg 4e489748 with specific blockers): `eslint app/apex components/apex lib/apex` clean; `tsc --noEmit` reports 0 errors in apex/ paths; `prettier --check` clean on all 3 new files.
