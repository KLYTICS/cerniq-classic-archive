- 2026-05-16 — **security(report-artifact) + audit(IDOR_RESIDUAL_AUDIT): close 1 real cross-tenant by-id IDOR + document 8 by-design legit sites — output-trust audit complete.** Follow-on sweep after R3 v3 (`verify-body-trust`, `c8c8a3c3`) closed the input-side DTO surface and R5 (`verify-userid-chain`, `053e1832`) locked the canonical `req.user` identity chain — the natural next perimeter is the output-side by-id read surface (`findFirst`/`findUnique` against `where:{id}` in service files without tenant key in the WHERE clause). Regex `this\.prisma\.\w+\.(findFirst|findUnique)\(\s*\{\s*where:\s*\{\s*id\s*[:,}]` across `src/` excluding `*.spec.ts` surfaced **9 sites**. After per-site classification: 8 are by-design legit, 1 was a real IDOR. **Real IDOR closed — `backend-node/src/alm/reports/report-artifact.controller.ts:64-72` `GET /api/report-artifacts/:id`**: class-level `@UseGuards(AuthGuard, InstitutionScopeGuard, RolesGuard)` was SET, but the by-id URL lacks a `:institutionId` param for `InstitutionScopeGuard.canActivate` to scope on — the guard had no tenancy key in the path so it allowed through. Earlier `IDOR_RESIDUAL_AUDIT.md` entry ("✅ already class-level guarded") was a **false clean** — guard-presence was confused with guard-effectiveness. Cross-tenant artifact metadata (storage locator, content checksum, format, institutionId, modelLineage) was readable by any authenticated `OWNER`/`ANALYST` who could guess an artifact UUID. **Structural fix:** inject `InstitutionScopeGuard` into `ReportArtifactController` constructor (NOT via `@UseGuards` — that pattern requires URL param presence) with type-rationale comment naming the kernel primitive contract, then switch the handler to fetch-then-verify — load artifact, extract `userId` via canonical R5 chain (`req.user?.userId ?? req.user?.id ?? req.user?.sub`) + `isMasterCeo` flag, call `verifyOwnership(artifact.institutionId, userId, isMasterCeo)` BEFORE returning. NotFoundException from the service surfaces uniformly as 404 to prevent cross-tenant existence-enumeration via timing-channel. **Paired spec (`report-artifact.controller.spec.ts`, +3 tests, 10/10 green):** (a) happy-path locks `verifyOwnership` invocation with the artifact's `institutionId` + caller userId + isMasterCeo:false; (b) wrong-tenant call propagates `ForbiddenException` (verifyOwnership.mockRejectedValueOnce → controller rethrow); (c) master-CEO flag forwarded so platform admins bypass the tenant check. **8 legit by-id sites documented** (no code changes, audit only — each carries a distinct semantic justification): `agent-api/alerts.controller.ts:167` (in-method tenant compare on line 156), `portal/portal.controller.ts:760` (self-lookup of authenticated user by canonical userId), `leads/lead-scoring.service.ts:39` (caller `leads.controller.ts:148` is `@UseGuards(AdminKeyGuard)` admin-only), `agents/runner/agent-run.service.ts:220` (callers `agents.controller.ts:117/124` use the fetch-then-verify-via-assertRunOwnership pattern), `alm/reseller.service.ts:47` (global CPA-firm catalog), `governance/governed-scenario.service.ts:35` + `governance/governed-benchmark.service.ts:39` (global catalogs with intentionally-globally-unique keys), `feedback/feedback.controller.ts:104` (public auth-surface where the row IS the auth check, carries `// verify:auth-skip`). **Audit doc updates (`docs/security/IDOR_RESIDUAL_AUDIT.md`):** new section "Output-trust by-id audit (2026-05-16) — 1 real IDOR closed, 8 by-design legit" with the full 9-row classification table + per-site rationale + the explicit "false-clean correction" callout for the report-artifact row; existing matrix row updated from `✅ already class-level guarded` to `✅ (post-fix)` with cross-reference to the new section. **Why no R4 verifier (yet):** at 8/9 legit and 1/9 real, a static "by-id without tenant key in WHERE" rule would emit 88% false-positive rate. Each legit case has a distinct semantic reason (global catalog / public auth-surface / fetch-then-verify / in-method compare / admin-only / self-lookup) — encoding all of them as detection patterns plus skip annotations is a high-noise / low-marginal-signal ratchet at current sample size. The audit-doc record + manual review at each new `findFirst({where:{id}})` introduction is the right shape until 2+ more real IDORs of this shape surface. **Routes intentionally NOT changed:** `findByChecksum` (content-checksum-derived reverse lookup is a verification feature, checksum is not guessable) + `verify` (confirmation oracle only matters if caller already has the content) — both noted in the new audit section with their threat-model rationale. **Trio invariants on commit:** verify-userid-chain 60/0, verify-body-trust 64/512/36/0, verify-auth-coverage 60/512/453+48/0, verify-institution-scope-guard 132/132+12/12+11/11+1/1/0, report-artifact.controller.spec.ts 10/10. **Cross-session coordination:** lane claimed `cerniq:R4-output-trust-audit-artifact-getById-IDOR-closure` via `claude-peers`; explicit-path `git commit --only` scoped to 4 paths (artifact controller + spec + IDOR audit doc + this handoff entry); peer enterprise-audit-swarm's `1b7d04fc` (alm Rule 11 type-rationale + alm.controller.security.spec.ts) already landed and is the established pattern this commit's type-rationale comments mirror; peer market-data + apex-phase-2 + llm-bridge work all in their respective lanes. — `backend-node/src/alm/reports/report-artifact.controller.ts`, `backend-node/src/alm/reports/report-artifact.controller.spec.ts`, `docs/security/IDOR_RESIDUAL_AUDIT.md`
