- 2026-05-16 — **Cold-storage revival pair triaged (runbook + smoke).** `docs/ops/cold_storage_revival.md` §4 corrected from Cloudflare (wrong — cerniq DNS is Spaceship retail at `launch1/launch2.spaceship.net`) to Spaceship + Railway-TLS-issuance flow; new §4.2 covers the `_railway-verify.api` TXT + `x-railway-fallback` kill-switch the 2026-05-16 revival actually needed. Appendix B added — 2026-05-16 revival log captures Path F decision, new Railway project/service IDs (stable infra refs, not secrets), the `projectTokenCreate` GraphQL pattern for regenerating `RAILWAY_TOKEN` without leaking the value, and 5 lessons for the next revival cycle (incl. that §4 was wrong). Old Appendix B (Cost) → C. `scripts/pre-deploy-smoke.sh` first commit (was untracked) + `--self-test` per CLAUDE.md D24 ratchet point 4 — 11 embedded assertions verify check()/warn() helpers, runs <1s with no working-tree side effects, portable to macOS bash 3.2. Normal-mode smoke: 42/44 (one FAIL is peer `a17abd74`'s in-flight `alm.controller.ts` + `intelligence.controller.ts` work, surfaced as a smoke signal; peer messaged via `claude-peers msg 40eada1c`). Self-test mode: 11/11. SHA `a69e44fa`.
