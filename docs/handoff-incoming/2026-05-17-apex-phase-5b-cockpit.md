- 2026-05-17 — **Apex absorption — Phase 5b (/apex/cockpit shell with mocked status strip).** Second of four flagship-route ports. The original ApexCockpit (`apex/components/apex-cockpit.tsx`) is a 408-line composition over 25+ component imports (Header, PairRow, TabBar, AlarmSidebar, NewsTicker, CommandCenter, AutonomyTab, DalioEngine, MauldinEngine, SwarmView, MasterPromptsTab, MultiAssetOverviewPanel, CalibrationTab, StressTestTab, PropFirmsTab, ExecutionTab, SignalBibles, PlaybookTabContent, SystemHealth, QuickActionBar, RuntimeHealthStrip, WorkspaceStatusStrip) + 4 hooks (useLiveEngine, useRuntimeSafety, useSimulationHub, useWorkspaceSession) + 24 Zustand store selectors from useApexStore. Transitive port surface: 5,000+ LOC. Phase 5b ports the *visual shell* + the *two smallest live panels*. Added `frontend/lib/apex/cockpit-contracts.ts` (45 lines) — narrowed `Trade` (paper-trade ledger), `AutoRecommendation` (max daily trades + base risk), `CockpitMockState` aggregate. Added `frontend/components/apex/intraday-pnl.tsx` (124 lines after prettier; 53 lines source) — verbatim port of apex/components/intraday-pnl.tsx with inline `getSession()` helper (UTC-hour → FX-session classifier). SVG sparkline, color thresholds, monospace numerals preserved verbatim. Added `frontend/components/apex/risk-budget.tsx` (124 lines after prettier; 67 lines source) — verbatim port with same circular DD-gauge (32×32, 12-radius, 75.4 arc, 25% offset, green ≤50% / yellow ≤80% / red >80% thresholds), trades-remaining + capacity counters in Orbitron. Added `frontend/app/apex/cockpit/page.tsx` (~270 lines after prettier) — wraps in ApexPageShell, renders status strip with both ported panels driven by `MOCK_STATE` (4 paper trades, mixed CLOSED+OPEN, $64.50 intraday PnL, 6-trade daily budget at 1.0% base risk). 11-tab nav (COMMAND, EXECUTE, AUTONOMY, PLAYBOOK, DALIO, MAULDIN, SWARM, MASTER PROMPTS, STRESS TEST, CALIBRATION, PROP FIRMS) with client-side active-tab state; per-tab ApexEmptyState explains that tab content ports alongside the Zustand store + API handlers in Phase 6. TAB_DESCRIPTIONS map provides operator-grade copy for each tab. Verification scoped to Apex files (peer 72c0fb06 market-data WIP still blocks full lint): `eslint app/apex components/apex lib/apex` clean; `tsc --noEmit` reports 0 errors in apex/ paths; `prettier --check` clean on all 4 new files.
