- 2026-05-16 — **fix(alm) + test(alm): R3 v3 closure followup — Rule 11 type-rationale on `alm.controller.ts`'s 2 new `@Req() req: any` + paired `alm.controller.security.spec.ts` (8 tests locking the structural body-IDOR invariants closed by `c8c8a3c3`).** Two-part followup to `c8c8a3c3 security(alm) + verifier(R3): close 3 alm body-IDORs flagged by R3 v3 + reinstate R3 v3 strict`. **Part 1 — Rule 11 unblock (CI-critical):** `c8c8a3c3` added 2 new `@Req() req: any` parameters to `alm.controller.ts` (`createInstitution` + `saveCustomYieldCurve`) without `// type-rationale:` comments, lifting the file's unrationalized-`any` count from baseline 21 → 23. `verify-rule-11-any-rationale.mjs` runs only in `npm run lint` (not pre-commit, only in CI), so the regression landed on origin without surfacing locally. This commit adds two `// type-rationale: ...` lines on the immediately-preceding line of each new `@Req() req: any` — multi-line param formatting (split across 3 lines) is mandatory because Rule 11 detects the marker via `^\s*\/\/\s*type-rationale\s*:\s*\S+` against the IMMEDIATELY PRECEDING original line. Same escape-hatch pattern as `90c986d1 fix(ncua): add type-rationale on ncua.controller.ts catch to unblock verify:rule-11 regression` — D24 ratchet §3 says ceilings only LOWER without explicit justification, so rationale-comment beats baseline-bump. Rationale text: "NestJS Express Request shape varies with auth-strategy registration (Passport JWT vs. session vs. custom guard); we only read `user.{userId|id|sub}` plus `user.access.isMasterCeo` through optional-chaining, so over-typing a framework-injected mutable bag would force a maintenance-coupled local interface for ~3 field reads. Matches `ncua.controller.ts`:`@Req() req: any` pattern (baseline-3 era)." Verifier post-fix: 980 → 978 unrationalized hits, 212 files at-or-below baseline, **0 regressions** (was 1 regression on origin until this commit). **Part 2 — Paired security spec (enterprise-quality additive):** `c8c8a3c3` shipped IDOR fixes but only resignatured 3 existing tests in `alm.controller.spec.ts` to pass `(req as any, dto as any)`. Those updates keep compilation green; they do NOT lock the structural invariants. New `backend-node/src/alm/alm.controller.security.spec.ts` mirrors `ncua.controller.security.spec.ts` (`58651c54`) with 8 tests across the 3 closed endpoints: (a) `createInstitution` call-order lock proving `scope.verifyWorkspaceOwnership` runs BEFORE `almEnterprise.createInstitution` via `mock.invocationCallOrder`; (b) `createInstitution` Forbidden propagation never reaches `almEnterprise.createInstitution`; (c) `createInstitution` forwards `isMasterCeo=true` to the guard for master-CEO callers; (d) `saveScenario` call-order lock against `scenarioPersistence.saveScenario`; (e) `saveScenario` NotFound propagation never reaches the service; (f) `saveCustomYieldCurve` call-order lock against `yieldCurve.saveCustomCurve`; (g) `saveCustomYieldCurve` Forbidden propagation never reaches the service; (h) `saveCustomYieldCurve` reads userId from legacy `req.user.id` shape when canonical `userId` is absent (defense for older session payloads). Uses the proxy-mock direct-construction scaffolding from `alm.controller.spec.ts` because Nest's `Test.createTestingModule()` compile times out under AlmController's 88-dep DI fan-in; spec carries `@ts-nocheck` for the same reason the parent spec does — direct-construction args-array bypasses TS structural checks. `paramCount - 1` slot is the canonical `InstitutionScopeGuard` injection point (last constructor param per the audit-doc-prescribed pattern; see `c8c8a3c3`'s constructor comment). Why these 8 tests and not 7 like NCUA: NCUA covers 1 endpoint with 7 invariants; this spec covers 3 endpoints with a representative slice of each invariant, totaling 8 — call-order locks for all three, plus one Forbidden + one NotFound + master-CEO forward + legacy-id-shape spread across the suite. **Verification:** `npx jest src/alm/alm.controller.security.spec.ts src/alm/alm.controller.spec.ts` 164/164 green (156 prior + 8 new); `verify-no-orphan-spec` 418 spec files / 12 suffix-strip pairings / 0 orphans (`.security.spec.ts` suffix-stripped to `alm.controller.ts` automatically); `verify-body-trust --strict` unchanged 64/510/36/0; `verify-rule-11-any-rationale` 0 regressions; rest of `npm run lint` unchanged from pre-c8c8a3c3 baseline. **Pre-existing on origin (not in this lane, flagged for owner peer):** `9f5c6677 feat(ai-advisor): KLYTICS Rule 9 prompt + cost provenance` and `6892d7e6 feat(impact-extractor): KLYTICS Rule 9 prompt + cost provenance` introduced 3 TS2345 errors in `tsc --noEmit` — `extractUsage()`/`mergeUsage()` helper signature is too narrow vs. the Anthropic SDK `Message & {_request_id?}` actual return shape at `ai-advisor.service.ts:229`, `:432` and `impact-extractor.service.ts:57`. Pre-commit `tsc` is warn-only so it didn't block c8c8a3c3 either; CI `tsc --noEmit` step is red on origin pending the owning peer's fix. **Cross-session protocol:** I originally claimed the entire `cerniq:alm-tools+ai-advisor+verify-rule-9` lane and then `cerniq:R3-v3-closure (verify-body-trust + 3 alm IDOR fixes + 4 intelligence skips)`; both lanes were entirely landed by peers (`05f40871`, `9f5c6677`, `da34b7ff`, `c8c8a3c3`) during my read/prep windows — textbook shared-tree absorption that the `docs/handoff-incoming/` pattern (also landed during this session in `da34b7ff`) is specifically designed to prevent for the §5 surface. My genuinely additive contribution narrowed from "the lane" to "what c8c8a3c3 left undone": the CI-blocking Rule 11 regression and the missing paired security spec. Commit lands via `git commit --only` scoped to exactly 3 paths (`alm.controller.ts`, `alm.controller.security.spec.ts`, this handoff entry) so no peer index activity is absorbed. — `backend-node/src/alm/alm.controller.ts`, `backend-node/src/alm/alm.controller.security.spec.ts`
