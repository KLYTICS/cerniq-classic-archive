- 2026-05-17 — **`audit-klytics` canon-doc reconciliation — surfaces a real score-arithmetic drift in the canon doc.** Round-5 audit of `1ac1d103` (the round-4 aggregator). The aggregator implements the canon doc's stated formula honestly ("count of ✅ + 0.5×🟡, treating — as full credit") which for CerniQ yields **11 / 12 PASS** (10 ✅ + 2 🟡 = 11 numerator; 12 denom because zero — entries means zero removed). The canon doc displays **CerniQ: 10/11** — off by one on both numerator and denominator. Same off-by-one shape on Apex (3.5/12 actual, 3.5/11 displayed). AEGIS and ComplyKit are correct because their — entries genuinely reduce the denom. This commit does NOT unilaterally fix the cross-product canon; instead it makes the discrepancy visible at every review: new `parseCanon()` reads the matrix + score line, aggregator main reports both numbers with a yellow `⚠ drift` marker when they disagree, summary form flags inline (`KLYTICS audit: 11 / 12 PASS (canon doc displays 10 / 11 — drift)`). Self-test gains 3 cases verifying the parser locates 12 rule rows + extracts the score + recovers Rule 12 = ✅. 30/30 total assertions PASS. Reconciliation can go either direction (fix doc's displayed score, or change formula to match displayed convention) — both valid product-owner decisions. The Rule-10 lesson holds: two independent expressions of the same formula side-by-side reveal which one drifted. SHA `0517c945`.
