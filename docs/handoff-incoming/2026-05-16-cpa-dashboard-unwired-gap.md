- 2026-05-16 — **security(reports) + canon(Rule 1): convert CPA dashboard's silent empty arrays (`recentAlerts:[]`, `upcomingExams:[]`) into typed `UNWIRED_INTEGRATION` DataGap entries; close 2 long-standing `// TODO: wire up …` sites at `backend-node/src/cpa/cpa-client.service.ts:245-246`.** Closes a textbook Rule 1 (no silent zeros) violation: `getClientDashboard()` was returning `recentAlerts: []` and `upcomingExams: []` with a TODO comment instead of the canonical `null` + matching `gaps[]` entry. A CPA auditor reading "0 alerts" on the dashboard concludes there are no alerts to act on — the actual situation is "the alert pipeline integration has not been built yet." That misreading is the exact failure mode the DataGap convention exists to eliminate (D1, 2026-04-07; KLYTICS Rule 1 canonised 2026-05-15). **Canon extension:** added a new `UNWIRED_INTEGRATION` reason code to `DataGapReason` union in `backend-node/src/alm/reports/data-gap.ts`, with rationale comment distinguishing it from the existing `MISSING_*` codes: `MISSING_*` means "we tried to compute this for THIS institution and the inputs weren't loaded yet" (institution-local, will resolve on data ingest); `UNWIRED_INTEGRATION` means "no institution will ever populate this until the upstream system is built" (codebase-wide, will resolve on engineering work). The two modes route to different UI treatments and different operator escalation paths — keeping them as distinct reason codes preserves that signal. **Type widening:** `CpaDashboardData.recentAlerts` and `upcomingExams` widened from `any[]` to `unknown[] | null` (the only assigned value today is `null` — `unknown[]` reserves room for the future alert + exam-summary DTOs without claiming a shape we don't yet have). Also added required `gaps: DataGap[]` field to the interface. The `any[]` → `unknown[]` narrowing avoids Rule 11 friction (no new `any` introduced; existing `any` baseline unaffected). **Spec:** new `it('emits UNWIRED_INTEGRATION gaps for unwired alert + exam-prep feeds')` at `backend-node/src/cpa/cpa-client.service.spec.ts:312` locks the convention — asserts `recentAlerts === null`, `upcomingExams === null`, `gaps.length === 2`, both gaps carry reason=`UNWIRED_INTEGRATION` + severity=`WARNING` + populated `action` string. Existing 3 getClientDashboard tests untouched (their assertions are field-specific via `expect(dashboard.X).toBe(...)`, not deep-equal, so the new fields don't break them). **Follow-up — Frontend mock-data leak (NOT in this commit, flagged for next session):** `frontend/app/portal/cpa-dashboard/page.tsx` is the worse half of this problem and warrants its own lane. Three issues at file:line refs: (1) `DEMO_METRICS = { upcomingExams: 3, recentAlerts: 7 }` (line 70-74) is the initial state of the `metrics` `useState`, so the dashboard renders "3 upcoming exams, 7 recent alerts" BEFORE any fetch completes — a real CPA seeing those numbers on first paint would treat them as real; (2) the fetch catch block at line 129-131 silently swallows errors with `// Use demo data` and never surfaces the failure, so a 500 from the backend leaves "Rodriguez & Associates CPA" + 6 fake institutions on screen indefinitely; (3) frontend's `DashboardMetrics.upcomingExams: number` (line 28-29) mismatches backend's `unknown[] | null`, so `setMetrics(dashData.metrics)` would assign an array to a number-typed field and React would render `[]` as empty string in the prominently displayed `<p className="text-2xl font-bold">`. The proper fix is: drop all `DEMO_*` constants from the page module, switch to react-query with a loading state that doesn't render fake numbers, type-match frontend to backend, and render gap-aware UI for the new `gaps[]` field (the same `—` + tooltip convention from §4 of `SESSION_HANDOFF.md`). Backend-only is a prerequisite for that work — without the gap-aware backend response, the frontend has nothing to render gap UI from. **Trio invariants on commit:** verify-rule-11-any-rationale unchanged (no new `any`; widened `any[]` to `unknown[] | null` actually shrinks the baseline by 2 sites if the rule re-baselines; existing baseline file untouched here); verify-no-orphan-spec stays green (cpa-client.service.spec.ts pairing unchanged); existing 3 getClientDashboard tests still green (assertion shape unchanged); new gap test passes. — `backend-node/src/alm/reports/data-gap.ts`, `backend-node/src/cpa/cpa-client.service.ts`, `backend-node/src/cpa/cpa-client.service.spec.ts`
