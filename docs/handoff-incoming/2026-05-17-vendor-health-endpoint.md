- 2026-05-17 — **feat(vendor): runtime health endpoint — `GET /api/vendors/health` with per-vendor liveness probes.** Sixth commit in vendor-connectivity arc (after `05ebe844` Tier-2). Transforms `/admin/vendor-status` from declarative-only registry display into a live operational view. **VendorHealthService** (`backend-node/src/vendor/vendor-health.service.ts`, ~220 LOC + 14/14 spec): dispatches one probe per vendor in parallel via `Promise.allSettled`, summarizes counts (ok / degraded / unreachable / notConfigured / notProbed), returns ISO-timestamped envelope. Three probe strategies, picked per vendor: (1) **HTTP probe** — Yahoo (`/v6/finance/quote?symbols=AAPL`), CoinGecko (`/api/v3/ping`), Treasury Fiscal Data (`/avg_interest_rates?page[size]=1`), ECB SDW (`/EXR/D.USD.EUR.SP00.A?lastNObservations=1`). 4s hard timeout via AbortController; `>1500ms` = DEGRADED; non-2xx = UNREACHABLE with HTTP status in message; network error = UNREACHABLE with error message. (2) **Env-only probe** — FRED, Alpha Vantage, SEC EDGAR: check env var presence without hitting upstream (Alpha Vantage's 25/day budget can't afford a health-check burn; FRED key validates against state but probe spends a request). Returns NOT_CONFIGURED if any required env missing with explicit missing-var list. (3) **Configured-only probe** — Refinitiv RDP, Bloomberg HAPI: calls `isConfigured()` from the provider, NEVER test-calls the paid upstream. Returns OK with message `paid-enterprise credentials present — not test-called to avoid cost` when configured. **Scaffold vendors** (ICE BofA / Symitar / COSSEC / NCUA 5300 / planned vendors): return env-only probe with `notProbedReason: 'scaffold or planned — no probe endpoint defined'` → state NOT_PROBED. **Rule 1 compliance:** every degrade path produces a structured state + explicit message; latencyMs is `null` (not 0) when the probe couldn't measure (env missing, scaffold, network error). No silent zeros. **Endpoint** (`backend-node/src/vendor/vendor.controller.ts`): `GET /api/vendors/health` public — read-only liveness summary, no auth required, no secrets in payload. Carries `verify:auth-skip` comment for the auth-coverage verifier. Returns `VendorHealthSummary`: `{ total, ok, degraded, unreachable, notConfigured, notProbed, vendors: VendorHealthEntry[], generatedAt }`. **VendorModule wiring:** VendorHealthService added to providers + exports. VendorController DI-injects the service via constructor. **KLYTICS discipline (all 4 verifiers GREEN at commit time):** Rule 1 — every state branch produces null latency + structured message instead of fake 0. Rule 4 — no audit_log mutations. Rule 7 — health entries carry `vendorId` lineage; summary carries `generatedAt` timestamp. Rule 11 — `// type-rationale:` on 1 fetch+AbortController error narrowing site; baseline unchanged at 213/979. Rule 12 — no randomness. **Test coverage:** 14/14 new spec covers all five health states + counts-bucketing + degraded threshold + ISO timestamp + per-vendor env-driven state transitions. Cumulative 104/104 across `src/vendor` suite. **Operational meaning:** an SRE / ops engineer can hit `curl https://api.cerniq.io/api/vendors/health` and immediately see which vendors are configured + responsive vs. which are silently down — without any frontend rendering. The future admin /vendor-status page enhancement (next commit) overlays the health column on the existing registry display. — `backend-node/src/vendor/vendor-health.service.ts`, `backend-node/src/vendor/vendor-health.service.spec.ts`, `backend-node/src/vendor/vendor.module.ts`, `backend-node/src/vendor/vendor.controller.ts`
