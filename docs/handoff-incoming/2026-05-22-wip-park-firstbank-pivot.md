- 2026-05-22 — **WIP park: scheduler kill-switch + IO budget indexes + pool tuning, landed as a single park commit ahead of FirstBank work.** Erwin is pivoting context to FirstBank; this entry preserves the in-flight Cerniq state on `claude/enterprise-quality-hardening` so the next Cerniq session can pick up without archaeology. Three themes ride in this commit and none of them are finished:

  **Theme 1 — Unified background-job kill-switch** (`backend-node/src/common/scheduler/background-job-gate.util.ts` + `.spec.ts`). New umbrella helper `areBackgroundJobsDisabled(env)` consumed by 8 sites: `agents/scheduler/scheduler-flag.util.ts`, `email/email-sequence.processor.ts`, `email/email.module.ts`, `intelligence/intelligence.module.ts`, `intelligence/intelligence.scheduler.ts`, `jobs/daily-pipeline.service.ts`, `jobs/jobs.module.ts`, `pipeline/pipeline-health.monitor.ts`, `pipeline/pipeline.module.ts`, `pipeline/pipeline.worker.ts`, `portal/demo-seat.sweeper.ts`, `portal/portal.module.ts`. The intent (inferred from the wiring pattern) is a single env flag that short-circuits every cron/worker/queue consumer so a single-process Railway dyno can be put into "API-only" mode for incident response. **Remaining work**: confirm the env-var name is consistent across all sites; add a `verify:background-job-gate` lint rule similar to the existing scheduler-flag pattern; document the flag in `docs/ops/railway_env_vars.md`; write an integration test that asserts no `@Cron` or BullMQ consumer fires when the gate is on.

  **Theme 2 — IO budget safety indexes** (`backend-node/prisma/migrations/20260518125000_io_budget_safety_indexes/migration.sql` + matching `schema.prisma` changes, +7 lines). Append-only migration adding indexes intended to bound the worst-case query plans on the IO-budget hot path. **Remaining work**: validate against staging with `EXPLAIN ANALYZE` on the queries these indexes are meant to back; if planner doesn't pick them, add `(\@@index)` hints or reconsider column order; update `backend-node/jest.coverageThreshold` only if new model code shipped alongside (none here).

  **Theme 3 — DB pool tuning + env hardening + ops docs** (`prisma.service.ts` lowers `DATABASE_POOL_SIZE` default 20→5; `config/env.schema.ts` +6 lines; `.env.example`, `.env.production.template`, `backend-node/.env.example` updated; ops docs refreshed: `deployment_runbook.md` +25 lines, `railway_env_vars.md` +17 lines, `resend_dns_setup.md` +63 lines, `disaster_recovery.md` reorg). The pool-size drop is the load-bearing change — Railway's free-tier Postgres has a hard cap that 20-per-process exceeded once horizontal scale crossed 1 dyno. **Remaining work**: verify the 5-per-dyno math against the actual Railway Postgres `max_connections` after the next deploy; the env-schema additions need a matching update to `docs/ops/railway_env_vars.md` (some may already be there — diff to confirm).

  **Also bundled**: `frontend/e2e/production-critical.spec.ts` +3 lines, `frontend/vercel.json` + root `vercel.json` 1-line each, `prisma.service.spec.ts` mirror-update for the pool default, `tsconfig.build.json` 1-line. These are incidental to the three themes.

  **Pickup recipe for next Cerniq session**:
  1. `git checkout claude/enterprise-quality-hardening && git pull`
  2. Read `docs/handoff-incoming/2026-05-18-dns-cutover.md` (DNS work already operational, just needs to be squashed into §5)
  3. Read this entry
  4. Decide: split the park commit into the three themes for clean review, or push the umbrella through as one
  5. Run `cd backend-node && npm run lint` — if `verify:background-job-gate` doesn't exist yet, that's the first ratchet to write
  6. Run `cd backend-node && npm run agent-eval` to confirm baselines still pass with the new background-job gate wired through scheduler-flag
  7. Squash both handoff-incoming files with `node scripts/squash-handoff-incoming.mjs` once the themes are properly landed

  **Why this is parked, not finished**: Erwin is switching to FirstBank work; no time today to ratchet `verify:background-job-gate`, validate the IO budget indexes against staging EXPLAIN plans, or write the integration test that proves the kill-switch actually silences every `@Cron`. Better to park cleanly with full context than half-ratchet and forget the rest.
