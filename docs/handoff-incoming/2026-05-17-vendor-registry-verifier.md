- 2026-05-17 — **feat(verify): vendor-registry consistency verifier — discovers 5 drift bugs on first run, locks the registry against silent rot.** Eighth commit in vendor-connectivity arc (after `33b6a414` fallback wiring). Adds a 4-rule consistency check on `backend-node/src/vendor/registry.ts` that runs as part of `npm run lint` (and therefore CI). **Rules enforced:** (1) every entry with `providerPath` points to a file that exists on disk; (2) every entry with status='production' or status='beta' MUST declare a providerPath (you can't be production without source code); (3) every entry with status='scaffold' MUST declare a blockedBy reason (so the chip-away surface is actionable); (4) no two entries share the same `id`. **Verifier mechanics** (`backend-node/scripts/verify-vendor-registry.mjs`): regex-anchors on `export const VENDOR_REGISTRY` to skip stray comment refs that previously caused the parser to anchor at line 11 instead of line 87. Tracks brace depth to delimit each object literal in the array. Per-entry field extraction via three small regexes (`id`, `status`, `providerPath`, `blockedBy`). Embedded `--self-test` flag with 5 fixtures covers all 4 rules + a duplicate-id case; CI runs the self-test before trusting the live check, following the project's D24-ratchet convention. **Drift bugs surfaced on first run (5):** `ice-bofa` → `vendor/macro-rates/ice-bofa.provider.ts` (didn't exist); `symitar-jack-henry` → `vendor/core-banking/symitar.provider.ts` (didn't exist); `cossec-efiling` → `vendor/regulator/cossec.provider.ts` (didn't exist); `ncua-5300` → `vendor/regulator/ncua-5300.provider.ts` (didn't exist); `verafin` → `vendor/aml-kyc/verafin.provider.ts` (didn't exist). All 5 were aspirational paths from the original framework commit (`db410efe`) where the registry described "where the provider WILL live" rather than "where it does live now." Fix applied: the 4 scaffold entries (ICE BofA, Symitar, COSSEC, NCUA 5300) repoint to `vendor/scaffolds.ts` (where their classes actually live); the 1 planned entry (Verafin — no scaffold class either) has providerPath removed entirely. **Wiring:** `npm run verify:vendor-registry` shortcut added; `npm run lint` chain extended to include it as the 10th step (after rule-12, before tsc). CLAUDE.md invariant-suite line updated. **Why this lock matters:** the registry feeds `/admin/vendor-status` (operator view) AND `/api/vendors/health` (probe dispatch). A registry pointing at a non-existent provider file silently misleads operators about what's wired. Catching this in CI — not at runtime when a peer notices a missing-file 404 — is the difference between a quality-locked registry and a documentation graveyard. **Future expansion path:** when each scaffold gets promoted to its own file (e.g. ICE BofA grows past ~80 LOC after creds arrive and gets split out per the scaffolds.ts comment guidance), the registry just needs its providerPath updated and this verifier catches drift in the opposite direction (new file written but registry still points at scaffolds.ts). **All 4 KLYTICS verifiers + new vendor-registry verifier GREEN at commit time.** Test count unchanged (no source code changed — just registry providerPath repointing + verifier addition). — `backend-node/scripts/verify-vendor-registry.mjs`, `backend-node/src/vendor/registry.ts`, `backend-node/package.json`, `CLAUDE.md`
