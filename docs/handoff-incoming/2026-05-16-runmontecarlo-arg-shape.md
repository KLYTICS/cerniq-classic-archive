- 2026-05-16 — **fix(agents): close runMonteCarlo silent-arg-ignore IDOR (wave-3 alm-tools cast strip) + widen schema to expose full Vasicek surface.** Third wave of the `alm-tools.ts` `as any` audit (after `97b1c4a4` + `31a2883f` wave-1 phantom-method/typo fixes and the wave-2 `getHealthScore`/`getCapitalAdequacy` cast strips locked by `alm-tools.spec.ts`). Pre-fix: `(this.monteCarlo as any).runSimulation(institutionId, input.paths)` — the service signature is `runSimulation(institutionId, opts?: { paths?, quarters?, kappa?, theta?, sigma? })`, so JS evaluated `(numericValue)?.paths` as `undefined` and the service silently fell back to its 10_000-path default regardless of caller intent. Tests passed because no spec inspected the actual path count in the result. **Fix:** (1) drop the `as any` and pass a typed opts object `{ paths, quarters, kappa, theta, sigma }`; (2) widen the Zod input schema to expose `quarters/kappa/theta/sigma` — the full Vasicek parameter surface the service already accepts but no agent could reach; (3) schema clamps mirror the service's internal clamp envelope (`MAX_PATHS=100_000`, `MAX_QUARTERS=120`, `kappa∈[0,5]`, `theta∈[-0.05,0.3]`, `sigma∈[0.0001,0.1]`) so Zod rejects out-of-range values at the agent boundary (loud) rather than the service silently clamping (quiet); (4) defaults intentionally omitted on Vasicek knobs — `undefined` lets the service fall through to its calibrated `DEFAULT_PARAMS` (Fed Funds Q4 2025); (5) summary string now appends `, NQ horizon` when `quarters` is provided so the agent can verify its config landed. **Test coverage:** added 2 regression-lock tests to `alm-tools.spec.ts` following the existing phantom-method canary pattern: (a) `runMonteCarlo invokes MonteCarloService.runSimulation with an opts OBJECT (not a bare number) — wave-3 silent-arg-ignore lock` — calls handler with full opts, asserts service receives matching object AND `typeof secondArg === 'object'` so a future regression to a bare number fails loudly; (b) `runMonteCarlo omits undefined Vasicek knobs so service defaults apply (back-compat lock)` — calls handler with only `paths`, asserts the unspecified knobs surface as `undefined` in the opts object (not auto-defaulted at the agent boundary, which would shadow service calibration). Together: 7→9 tests in this file, 1.0s green. **Verification:** `npx jest src/agents/registry/tools/alm-tools` 9/9 ✓; `npx tsc --noEmit` shows no new diagnostics on the file (the line-735 `await has no effect` warning is pre-existing, unrelated to this surface). **Cross-session protocol:** scope `cerniq:realtime-gateway-pnl-idor` released after Task 8 closure via peer bundle `ed08147f`; this commit uses the new `docs/handoff-incoming/` queue pattern (peer's `2026-05-16-handoff-incoming` workflow) to avoid SESSION_HANDOFF.md stage-race absorption — landing-gate Form (2) accepts the incoming file as equivalent to a §5 bullet, the archivist peer squashes it into §5 in batch. — `backend-node/src/agents/registry/tools/alm-tools.ts`, `backend-node/src/agents/registry/tools/alm-tools.spec.ts`, `docs/handoff-incoming/2026-05-16-runmontecarlo-arg-shape.md`
