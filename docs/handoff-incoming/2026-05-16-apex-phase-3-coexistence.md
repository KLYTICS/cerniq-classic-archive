- 2026-05-16 — **Apex absorption — Phase 3 (data layer coexistence wiring).** Locks operator decision (2026-05-16): /apex/* coexists with the existing Apex Supabase project; tables stay separate from cerniq's Prisma schema. Added `frontend/lib/apex/supabase-config.ts` (44 lines) — namespaced env contract: `NEXT_PUBLIC_APEX_SUPABASE_URL` + `NEXT_PUBLIC_APEX_SUPABASE_ANON_KEY` (distinct from cerniq's own `SUPABASE_URL` for unified KLYTICS auth — the two projects must not collide). `isApexSupabaseConfigured()` + `readApexSupabaseConfig()` graceful-degradation helpers. Added `frontend/lib/apex/operator-hub-fetcher.ts` (60 lines) — `fetchOperatorHubFirstPaint()` returns `{ snapshot, reason }`. Reasons: `not_configured` / `deferred_to_phase_6` / `fetch_failed` / `configured`. Currently returns null with reason "deferred_to_phase_6" because composing OperatorHubFirstPaintSummary requires porting 524 lines of aggregation logic from `apex/lib/operator-hub/first-paint.ts` — that aggregation belongs alongside the 262 API handlers being ported in Phase 6. Updated `frontend/app/apex/hub/page.tsx` to consume the fetcher: `export const dynamic = "force-dynamic"`, async server component, falls back to `MOCK_HUB_SNAPSHOT` on null, exposes `data-apex-data-source` + `data-apex-fetch-reason` debug attributes for observability. ApexStatusPill swaps between "LIVE · APEX SUPABASE" (when fetch succeeds, Phase 6+) and "MOCKED · FALLBACK" (current state). Added `NEXT_PUBLIC_APEX_SUPABASE_URL` + `NEXT_PUBLIC_APEX_SUPABASE_ANON_KEY` to root `.env.example` (the tracked env doc; `frontend/.env.example` is gitignored) with explanatory section. Verification scoped to Apex files (peer 72c0fb06 has unrelated broken WIP in `frontend/lib/hooks/use-yield-curve.ts` + market-data files blocking full lint): `eslint app/apex components/apex lib/apex` clean; `tsc --noEmit` reports 0 errors in apex/ paths; `prettier --check` clean on all 3 new/modified files.
