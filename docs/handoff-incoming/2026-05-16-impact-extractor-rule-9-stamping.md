- 2026-05-16 — **feat(impact-extractor): KLYTICS Rule 9 prompt + cost provenance on regulatory impact extraction — shrinks Rule 9 baseline 4 → 3.** Fourth Rule 9 chip-away of the session (after `d1d42e97` analyst, `c6605953` advisor-v2, `9f5c6677` ai-advisor). `impact-extractor.service.ts` is the smallest LLM-calling service in cerniq (116 LOC, single `messages.create` call site at line 28, no spec) — the canonical minimal-blast-radius chip-away. Mirrors the analyst `ask()` stamping shape exactly: reuses `computePromptVersion` + `extractUsage` + `estimateCostCents` from `src/alm/analyst/`. **What changed:** (1) Promoted `IMPACT_EXTRACTOR_MODEL = 'claude-sonnet-4-20250514'` and `IMPACT_EXTRACTOR_SYSTEM_PROMPT` (the brief Spanish-language regulator-expert prompt) to module-level constants — previously both were inline literals, drift-prone between SDK call and any future fingerprint. (2) Before the call: `computePromptVersion({ model: IMPACT_EXTRACTOR_MODEL, systemPrompt })` fingerprints the (model, prompt) tuple. (3) After the call: `extractUsage(response)` → four-class usage record; `estimateCostCents(IMPACT_EXTRACTOR_MODEL, usage)` → integer-centi-cent cost (or `null` + `costMissingReason: 'NO_PRICING_DATA'` if model isn't priced). (4) Structured `logger.log({ event: 'rule-9-stamp', surface: 'impact-extractor.extract', publicationId, model, promptVersion, usage, costCents, latencyMs, pricingVersion | costMissingReason })` entry fires before the JSON parse + persistence path. (5) **Rule 1 compounds** — null usage or unknown model never silent-zeros on cost. **Note: this service has no spec** (`grep -l impact-extractor.service.spec.ts` returns nothing) — a separate Rule 8 (golden tests with drift detection) gap that pre-dates this commit; verifier is the substitute discipline for this commit's correctness, mirroring the pattern used for advisor-v2 + ai-advisor whose specs also don't exercise the LLM path because the dynamic `import('@anthropic-ai/sdk')` is unmockable in jest. **Verifier closure:** `scripts/verify-rule-9-stamping.mjs` `BASELINE_UNSTAMPED` entry for `ai/regulatory/impact-extractor.service.ts` removed (note read "lower-priority path, stamp on next wave when regulatory pipeline is touched" — done now since the chip-away pattern is well-established and the service is so small that stamping costs less than maintaining the baseline entry). Verifier reports `4 stamped · 0 skip-commented · 3 baselined · 0 unstamped` (vs. `3 stamped · 4 baselined` pre-PR, vs. `1 stamped · 6 baselined` at session start — **50% baseline reduction**). Remaining 3: `agents/runner/llm-bridge.service.ts` (highest-leverage; cascades to agent-runner), `alm/alm-advisor.service.ts` (v1 legacy, retire-or-stamp decision pending), `ai/ingest/nl-ingest.service.ts` (depends on ingest pipeline lineage wiring). **Cross-product consequence:** cerniq Rule 9 maturity-matrix cell stays 🟡 (3 services still in baseline) but the ratchet measured by `verify-rule-9` reads 4 stamped / 3 baselined — chip-away ratchet moves correctly per canon §3 footnote. Uses the new `docs/handoff-incoming/` landing-gate pattern (zero SESSION_HANDOFF.md stage-race surface). — `backend-node/src/ai/regulatory/impact-extractor.service.ts`, `backend-node/scripts/verify-rule-9-stamping.mjs`, `docs/handoff-incoming/2026-05-16-impact-extractor-rule-9-stamping.md`
