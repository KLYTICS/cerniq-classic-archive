- 2026-05-17 — **`scripts/pre-deploy-smoke.sh` wired into `npm run`.** Closes the round-4 gap from the cold-storage triage arc: smoke existed and worked (`a69e44fa` round-1, `1d57f774` round-2, `5b40f230` round-3) but was not callable via `npm run`, failing CLAUDE.md "done" point 3. Added `npm run smoke:pre-deploy` and `npm run smoke:pre-deploy:test` to `package.json` (matches the existing `smoke:*` namespace alongside `smoke:exports` / `smoke:preview` / `smoke:production`). Chained `smoke:pre-deploy` as the FIRST step of `npm run verify:local:critical` so structural-shape failures (missing runbook, missing Dockerfile, broken toolchain) fail in 30s instead of after 5+ min of lint+test+build. Updated `docs/ops/cold_storage_revival.md` §0 to reference the npm forms as canonical discovery path (bash form preserved as fallback) + §0 note that `verify:local:critical` implicitly includes the pre-deploy gate. Verified: `npm run smoke:pre-deploy:test` → 10/10 PASS; `npm run smoke:pre-deploy` → 42/0/2 PRE-DEPLOY GATE: READY. SHA `65eb2720`.
