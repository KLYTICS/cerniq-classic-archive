- 2026-05-16 — **Cold-storage revival runbook — round-3 audit.** Follow-up to `a69e44fa` (round-1) + `1d57f774` (round-2). Surface-level drift was easy to grep; interior-logic bugs are what round-3 catches. Four findings: (1) §2.6 Path R secret matrix expanded from 1→4 — `DATA_ENCRYPTION_KEY` was the only secret warned as restore-sensitive, but `JWT_SECRET` (sessions), `API_KEY_PEPPER` (customer API key HMAC — same class as `e602c1d7` hash-divergence), and `MASTER_ACCOUNT_PASSWORD` (master auth) ALL invalidate independent slices of restored state if regenerated. Replaced the single warning with a Path-F-vs-Path-R column table + named the `/Users/money/Desktop/cerniq/.env` and `backend-node/.env` recovery paths. (2) §2.4 `pg_restore` safety — added an empty-DB count-tables pre-check before `--clean --if-exists` so a misrouted DATABASE_URL doesn't silently wipe a live database. (3) §3.0 Vercel-truth-source — my round-2 over-claimed `capexcycle=prod` based on local `.vercel/project.json` files (gitignored, per-user link state, not source of truth); reframed as "local check necessary but not sufficient; Vercel dashboard Domains tab is authoritative." (4) §6 + §8 hardcoded `claude/enterprise-quality-hardening` decoupled to `$(git branch --show-current)`. Appendix B receives a round-3 lesson on the meta-pattern: surface drift greps, interior bugs don't; audit `env.schema.ts` for every restore-sensitive secret separately. Smoke now green (peer's TS work landed) — 42/0/2, gate READY; `--self-test` 10/10 PASS; diff secret-scan clean. SHA `5b40f230` on `claude/enterprise-quality-hardening`. Receipt for [[inter-session-protocol]] Rule 10 — each round documents the previous round's residual blind spot.
