- 2026-05-17 — **feat(vendor): full KLYTICS vendor framework — registry + 2 working providers + 6 contract-bound scaffolds + admin /vendor-status page.** Third commit in the vendor-connectivity arc (after `41c17e82` FRED + `def58670` multi-card expansion). Delivers "scaffold think plan implement" in one coherent commit: 17 PR-cooperativa-relevant financial vendors are now either fully integrated, DI-wired as typed scaffolds, or registered with explicit blocked-by context. **Vendor registry** (`backend-node/src/vendor/registry.ts`, ~350 LOC): declarative metadata across 10 categories (market-data, macro-rates, filings, core-banking, regulator-filing, aml-kyc, identity, payments, esg, peer-data). Each entry: id, name, description, category, status (planned/scaffold/beta/production), compliance posture (public/free-tier/paid-self/paid-enterprise/regulator/core-banking), engineer-day integration cost, env vars required, blocked-by reason, PR-cooperativa relevance rationale. 17 vendors documented: Yahoo Finance, CoinGecko, Alpha Vantage, Polygon.io, FRED, Treasury Fiscal Data, ECB SDW, Bloomberg BPIPE, Refinitiv Eikon, ICE BofA, SEC EDGAR, Symitar (Jack Henry), KeyStone (Corelation), COSSEC e-filing, NCUA 5300, Verafin (Nasdaq), LexisNexis Risk. **Working provider #1 — TreasuryFiscalDataProvider** (~180 LOC + 11/11 spec): US Treasury Fiscal Data API at `api.fiscaldata.treasury.gov`. No auth required. Returns same YieldCurveDto shape as FredProvider so consumers are byte-interchangeable. Wired as **FRED yield-curve fallback** in `MarketDataService.getYieldCurve()` — ladder is now [live-cache (≤1h) → FRED → Treasury Fiscal Data → stale-cache (<24h) → null DataGap]. Proves the multi-provider failover pattern: when FRED's key is missing or upstream is down, the yield-curve surface keeps working from the alternate Treasury distribution. Operators see a `warn`-level log naming the failover. **Working provider #2 — SECEdgarProvider** (~300 LOC + 11/11 spec): SEC EDGAR public filings API at `data.sec.gov`. Auth via SEC-required `User-Agent` header from `SEC_EDGAR_USER_AGENT` env. Methods: `getRecentFilings(cik)` for ~1000 recent submissions (10-K, 10-Q, 8-K, etc. with XBRL flags); `getCompanyConcept(cik, concept, taxonomy='us-gaap')` for XBRL time-series of any financial fact (Assets, Liabilities, Revenues, NetIncome). CIK zero-padding handled internally — callers use the natural form. Without UA: returns null gracefully (Rule 1). 11/11 spec covers padding, UA transmission, missing-UA degrade, 404-as-no-data, malformed body, network error, point-level filtering. **Scaffold providers** (`backend-node/src/vendor/scaffolds.ts`, ~270 LOC): six DI-wired NestJS `@Injectable()` classes — `BloombergBPipeScaffold` (getRealtimePrice, getCurve), `RefinitivEikonScaffold` (getRealtimePrice, getNews), `IceBofaScaffold` (getIndexLevel, getSectorSpread), `SymitarScaffold` (getMemberBalances, getLoanBook), `CossecScaffold` (submitFiling, getFilingHistory), `Ncua5300Scaffold` (submitCallReport, getCallReportHistory). Each method returns `null` + logged warn; each scaffold exposes `scaffoldStatus(): VendorScaffoldGap` returning `{ __dataGap: true, scaffold: true, vendor, vendorId, reason: 'CREDENTIALS_REQUIRED', envVarsRequired, action }` so callers surface honestly in the UI. When contract/sandbox/creds arrive, work is "replace stub body" — consumer code is already in place via DI. **VendorModule + VendorController + admin page:** module wires all 7 providers (1 working SEC EDGAR + 6 scaffolds); controller exposes `GET /api/vendors` and `GET /api/vendors/by-category` (public read-only — pure registry metadata). Wired into `app.module.ts`. Admin page at `/admin/vendor-status` is a Bloomberg-density operator tool — 4 status-count cards on top (production/beta/scaffold/planned of total), per-category sections below with vendor cards showing name + status pill + description + posture + cost + env vars + blocked-by reason + PR-cooperativa relevance + outbound vendor URL. Renders explicit DataGap state when `/api/vendors` fetch fails (no silent zero rendering). **KLYTICS discipline (all 4 verifiers GREEN at commit time):** Rule 1 — every working provider returns `T \| null`; every scaffold method returns null + logged warning + structured `VendorScaffoldGap` for UI surfacing. Rule 4 — no audit_log mutations. Rule 7 — every DTO carries `provider` + `seriesId`/`accn`/`cik` lineage breadcrumb. Rule 11 — `// type-rationale:` comments on 3 `error: unknown` catch-block narrowings; baseline unchanged. Rule 12 — no randomness. **Test coverage:** 307/307 across `src/market-data` + `src/vendor` green in 2.1s. **MarketDataModule + service + spec updated:** TreasuryFiscalDataProvider added to providers; `MarketDataService` constructor takes the new provider as 4th arg; service spec fixture extended with `mockTreasuryFiscalDataProvider`, 59/59 tests stay green. **What this does NOT include** (next vendor surfaces, ranked): real Bloomberg/Refinitiv/ICE BofA integration (each blocked on contract + sandbox), real Symitar PowerOn (per-cooperativa contract), real COSSEC e-filing (API docs + portal creds), real NCUA 5300 (CUOnline creds + current XSD). Framework is ready; each is a follow-up commit when the corresponding blocker clears. **Cross-product consequence:** cerniq now has a load-bearing vendor framework with 17 vendors documented, 2 new live providers, 6 typed scaffolds, and an admin visibility surface. The KLYTICS vendor connector pattern is canonical and reusable across ComplyKit / AEGIS / Apex when those products grow vendor needs. — `backend-node/src/vendor/registry.ts`, `backend-node/src/vendor/scaffolds.ts`, `backend-node/src/vendor/vendor.module.ts`, `backend-node/src/vendor/vendor.controller.ts`, `backend-node/src/vendor/filings/sec-edgar.provider.ts`, `backend-node/src/vendor/filings/sec-edgar.provider.spec.ts`, `backend-node/src/market-data/providers/treasury-fiscal-data.provider.ts`, `backend-node/src/market-data/providers/treasury-fiscal-data.provider.spec.ts`, `backend-node/src/market-data/market-data.service.ts`, `backend-node/src/market-data/market-data.service.spec.ts`, `backend-node/src/market-data/market-data.module.ts`, `backend-node/src/app.module.ts`, `frontend/app/admin/vendor-status/page.tsx`
