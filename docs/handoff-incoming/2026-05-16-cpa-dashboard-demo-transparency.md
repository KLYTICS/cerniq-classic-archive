- 2026-05-16 — **frontend(cpa-dashboard) + canon(Rule 1 pair): visible demo-mode banner + gap-aware "—" rendering on `recentAlerts` / `upcomingExams` metric cards; pairs with backend commit `8577c89a`.** Closes the second half of the CPA-dashboard Rule 1 violation flagged in `2026-05-16-cpa-dashboard-unwired-gap.md`. The backend commit made the API honest (silent `[]` → typed `UNWIRED_INTEGRATION` gaps); this commit makes the UI honest about whatever the backend tells it. **What was wrong:** `frontend/app/portal/cpa-dashboard/page.tsx` initialised `useState` with `DEMO_FIRM` / `DEMO_CLIENTS` / `DEMO_METRICS` and a silent `catch {}` block that fell through to those demo values whenever the backend was unreachable. A real CPA logging in during an API outage saw "Rodriguez & Associates CPA · 6 institutions · 3 upcoming exams · 7 recent alerts" rendered as if real — exact failure mode KLYTICS Rule 1 + the Roberto-auditor persona memo warn about. **Worse:** the page's existing test harness (`__tests__/page.test.tsx`) **enforced the anti-pattern** — `mockFetch.mockResolvedValue({ ok: false })` forced the silent-catch path on purpose and then asserted the demo values rendered (lines 44, 107, 127). The tests were load-bearing for the wrong invariant; removing demo data outright would have broken 9 green tests. **Two-pronged fix preserves the affordance, fixes the truthfulness:** (1) **`demoMode` state + visible banner** — new `useState<boolean>(false)`, flipped `true` in the `finally` block when either fetch did NOT succeed (`dashLive && clientsLive` guard). When true, a top-of-page amber banner with `role="status" aria-live="polite" data-testid="demo-mode-banner"` renders bilingual text: "Demo data — The backend did not respond. Institution names, metrics, and summaries shown are demo placeholders — they do not reflect your firm's real data." Mirrors the `preview-report.service.ts` watermark pattern for PDFs — same discipline (transparent fallback) different surface (DOM banner). (2) **Gap-aware `—` rendering on the two metric cards** — new `DataGap` interface (mirrors backend `DataGap` shape via `field` / `reason` / `severity` / `action?` projection; kept inline because the dashboard only consumes this projection and full type lives backend-side as source of truth); new `findGap(gaps, field)` helper; new `gaps: DataGap[]` state populated from `dashData.gaps` when the backend returns the new field. When the Upcoming Exams / Recent Alerts cards find a matching gap, they render `—` (em-dash) in `text-slate-400` with `title={gap.action}` for native tooltip and `data-testid="upcoming-exams-gap"` / `data-testid="recent-alerts-gap"` for test selection. Otherwise they fall through to the existing numeric rendering (live data path; demo path when in demo-mode). **Tests:** all 11 pass (9 existing untouched + 2 new). Existing tests still work because (a) the banner addition is visible-but-not-blocking, (b) demo data still renders when `mockFetch` returns `ok: false`. New test 1 — `'shows a demo-mode banner when the backend fetch fails'` — asserts `getByTestId('demo-mode-banner')` + 'Demo data' text + that 'Rodriguez & Associates CPA' (demo firm) ALSO renders (banner augments demo, doesn't replace it). New test 2 — `'renders "—" gap indicators when backend returns UNWIRED_INTEGRATION gaps'` — overrides `mockFetch` with `ok: true` + a full response containing `gaps[]` matching the backend's two `UNWIRED_INTEGRATION` entries; asserts both gap-test-ids carry `—` text content and `title` attribute set to the gap's `action` string, that NO demo banner is present (both fetches were ok:true → `demoMode === false`), and that the old hardcoded "3" / "7" silent-zero values are NOT in any `p.text-2xl` element. **Prettier normalization caveat:** the existing `page.tsx` (483 lines) had single-quote drift versus the frontend's prettier config (double-quote convention). `npx prettier --write` normalized the whole file as part of this commit, so the visual diff is ~400 lines of `'foo'` → `"foo"` plus the actual ~60-line logic delta (new interface + helper + state + banner JSX + gap-aware card wrappers). Frontend prettier is NOT in the husky pre-commit chain (only backend is — see CLAUDE.md "Pre-commit (husky) — staged-file checks only" §), so drift accumulates silently on frontend until someone touches a file. This commit pays down the drift on the one file it edits; broader frontend prettier sweep is a separate lane. **Cross-peer signal (not in this commit, propagated):** `npm run lint` on frontend currently fails on a different file — `verify-alm-registry: error [registry] app/alm/market-rates/page.tsx exists but is not registered in lib/alm/registry.ts. Add an entry to ALM_MODULES`. That's from peer `72c0fb06`'s FRED yield-curve commit `41c17e82` (the `/alm/market-rates` page they added needs a paired registry entry). Peer has released their claim; this is now an orphan follow-up. Flagged here so whoever next picks up frontend hardening or runs `verify:local:critical` knows where to look — it's a 1-line addition to `frontend/lib/alm/registry.ts`'s ALM_MODULES array. — `frontend/app/portal/cpa-dashboard/page.tsx`, `frontend/app/portal/cpa-dashboard/__tests__/page.test.tsx`
