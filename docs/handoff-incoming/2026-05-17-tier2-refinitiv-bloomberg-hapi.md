- 2026-05-17 — **feat(vendor): Tier-2 Refinitiv RDP + Bloomberg HAPI providers — real REST call paths, null when creds missing.** Fifth commit in the vendor-connectivity arc (after `41c17e82` FRED → `def58670` multi-card → `db410efe` framework → `314dc75e` Tier-1 ECB SDW + Alpha Vantage → now). Promotes the two highest-leverage paid-enterprise vendors from `scaffold` (stub-only) to `beta` (real REST surface, returns null when paid creds absent) with 52/52 spec coverage. **RefinitivEikonProvider** (`backend-node/src/vendor/market-data/refinitiv-eikon.provider.ts`, ~310 LOC + 30/30 spec): full OAuth2 client_credentials flow against `api.refinitiv.com/auth/oauth2/v1/token` with in-memory token cache + 30s pre-expiry refresh; `getAccessToken()` returns string|null with logged-error on every failure mode (missing creds / non-200 / missing access_token field / network error). `getRealtimePrice(ric)` calls `/data/pricing/snapshots/v1/?universe=<RIC>&fields=BID,ASK,TRDPRC_1,OPEN_PRC,HIGH_1,LOW_1,HST_CLOSE,NUM_MOVES,CF_VOLUME` and parses the 2D `values[row][field]` payload into the standard QuoteDto shape (interchangeable with Yahoo / Alpha Vantage callers). `getNews(query, limit)` calls `/data/news/v1/headlines` and parses into NewsArticleDto[]. RIC regex `/^[A-Z0-9.=:/\-]{1,30}$/i` accepts the documented Refinitiv grammar (`AAPL.O`, `EUR=`, `US10YT=RR`, `IBM.N`) while rejecting shell-meta. 401 from snapshot endpoint clears token cache so the next call re-auths. `isConfigured()` is a public diagnostic for admin pages — returns true iff `REFINITIV_APP_KEY` + `REFINITIV_APP_SECRET` are present, without leaking values. `parseSnapshot()` + `parseHeadlines()` are exposed (not private) so the defensive-parsing path is independently verifiable from the network plumbing. **BloombergHapiProvider** (`backend-node/src/vendor/market-data/bloomberg-hapi.provider.ts`, ~290 LOC + 22/22 spec): documented Bloomberg HAPI (Hypermedia API) REST scaffold. Names the protocol disambiguation in code comments — **BPIPE** (proprietary TCP binary via BLPAPI SDK), **HAPI** (REST for EDL customers, this provider), **DAPI** (desktop COM interop), **FIGI** (free reference-only) — so the next engineer doesn't waste a week wiring the wrong variant. OAuth2 client_credentials against `/eap/v1/oauth2/token` with ~3600s token caching. `submitRealtimePriceRequest(security)` POSTs a well-formed `DataRequest` envelope to `/eap/catalogs/<account>/requests/`, parses the 201 response `Location: /eap/catalogs/<acct>/requests/<id>/` header, returns the requestId. `buildDataRequest()` is exported so the envelope shape (universe/fieldList/trigger/formatType/pricingSourceOptions) is independently testable. Bloomberg ticker grammar regex `/^[A-Z0-9 .=:/\-]{1,50}$/i` accepts `AAPL US Equity`, `EURUSD Curncy`, `CT10 Govt`. **Async-poll gating:** `getRealtimePrice()` submits + logs requestId + returns null. The full response-download lifecycle is gated behind `BLOOMBERG_HAPI_ENABLE_ASYNC_POLL=true` env flag (default off — production-rollout gate). This is the *honest scaffold* posture: real path coded, real auth exercised, no fake data returned. When the env flag flips on (and the response-download path is implemented in a follow-up commit), `getRealtimePrice()` returns full QuoteDto. **Vendor module rewire** (`backend-node/src/vendor/vendor.module.ts`): `BloombergBPipeScaffold` + `RefinitivEikonScaffold` removed from imports/providers/exports; replaced with `BloombergHapiProvider` + `RefinitivEikonProvider`. Remaining four scaffolds (ICE BofA / Symitar / COSSEC / NCUA 5300) keep their pre-Tier-2 honest-null behavior pending their respective contract / portal / XSD blockers. **scaffolds.ts cleanup:** the two superseded scaffold classes deleted; replaced with a comment block naming the promotion + linking to the Tier-2 provider files. **Registry updates** (`backend-node/src/vendor/registry.ts`): `bloomberg-bpipe` status `scaffold` → `beta`, name updated to `Bloomberg (HAPI variant)`, env vars rewritten to the new HAPI set (`BLOOMBERG_HAPI_BASE_URL` / `CLIENT_ID` / `CLIENT_SECRET` / `ACCOUNT_ID` / `ENABLE_ASYNC_POLL`), providerPath corrected to `vendor/market-data/bloomberg-hapi.provider.ts`, integration cost dropped from 14 to 7 days (REST is cheaper than BLPAPI SDK install + connection). `refinitiv-eikon` status `scaffold` → `beta`, env vars simplified to `REFINITIV_APP_KEY` + `REFINITIV_APP_SECRET` (the RDP REST surface needs only OAuth2 credentials, not host + key), providerPath corrected, integration cost dropped from 14 to 5 days. **KLYTICS discipline (all 4 verifiers GREEN at commit time):** Rule 1 — every method returns `T | null` + logged warn on every degrade path (no creds, no token, no response, malformed parse). Rule 4 — no audit_log mutations. Rule 7 — every DTO carries `provider: 'refinitiv-eikon' | 'bloomberg-hapi'` + ric/security context. Rule 11 — `// type-rationale:` on 6 narrowing sites (process-local token cache, OAuth2 token response, fetch error, Bloomberg dynamic envelope, async-poll-not-implemented stub). Verifier passes 213/979 baseline unchanged. Rule 12 — no randomness. **Test coverage:** 52/52 new (30/30 Refinitiv + 22/22 Bloomberg HAPI); cumulative 391/391 across `src/market-data` + `src/vendor` suites. Type-check clean. **Vendor framework status post-commit:** 17 vendors total — 7 production (Yahoo, CoinGecko, FRED, Treasury Fiscal Data, ECB SDW, Alpha Vantage, SEC EDGAR) + 2 beta NEW (Refinitiv RDP, Bloomberg HAPI) + 4 scaffold (ICE BofA, Symitar, COSSEC, NCUA 5300) + 4 planned (Polygon, KeyStone, Verafin, LexisNexis). Production+beta combined: 9/17 (53%). **What this does NOT include:** Bloomberg HAPI async response-polling + CSV-download parse (gated behind `BLOOMBERG_HAPI_ENABLE_ASYNC_POLL`, needs real HAPI account before metric-instrumenting); ICE BofA REST scaffold upgrade from stub; Symitar PowerOn + COSSEC e-filing + NCUA 5300 (each blocked on per-cooperativa contract or regulator API docs). — `backend-node/src/vendor/market-data/refinitiv-eikon.provider.ts`, `backend-node/src/vendor/market-data/refinitiv-eikon.provider.spec.ts`, `backend-node/src/vendor/market-data/bloomberg-hapi.provider.ts`, `backend-node/src/vendor/market-data/bloomberg-hapi.provider.spec.ts`, `backend-node/src/vendor/vendor.module.ts`, `backend-node/src/vendor/scaffolds.ts`, `backend-node/src/vendor/registry.ts`
