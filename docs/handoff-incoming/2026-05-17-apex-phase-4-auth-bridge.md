- 2026-05-17 — **feat(apex): Apex absorption — Phase 4 (auth bridge + sovereign stealth-404 gate).** Closes the auth-gate-shaped hole left by Phase 5c's intentional deferral (page comment at `frontend/app/apex/sovereign/page.tsx:6-8` named Phase 4 as the owner). Three new files + one surgical wire-up — pure-add, zero modifications to existing cerniq auth surfaces. **Architecture:** `frontend/lib/apex/auth-bridge.ts` (~115 lines) ports three primitives verbatim from `APEX/lib/server/identity-policy.ts` to preserve the security contract exactly — `normalizeStringSet()` (comma-split + trim + lower-case), `resolveSovereignFromGithubLogin()` (single source of truth for the sovereign grant), `isApexProtectedMode()` (DEMO ↔ PAPER/LIVE switch via `NEXT_PUBLIC_DEMO_MODE !== "false"`). Two cerniq-specific additions on top: `deriveApexSession(cerniqUser)` translates cerniq's `{id, email, name, githubLogin?}` → the Apex session shape (`ApexSessionUser` with `id/email/providerSubject/groups/githubLogin/role/roleSource`), and `loadApexSessionFromCookies()` is the server-side bridge — reads request cookies via `next/headers`, calls the backend `/api/auth/profile` endpoint (the same upstream `frontend/app/api/auth/session/route.ts` consumes), returns the derived Apex session or null. The IO is isolated to that one function so the pure derivation + sovereign resolution can be tested without a network mock and reused by Phase 6 controllers. `frontend/lib/apex/sovereign-gate.ts` (~55 lines) implements the stealth-404 page guard. **Stricter posture than Apex source per operator decision (2026-05-17):** every non-sovereign caller — authenticated or not — gets `ApexSovereignNotFoundError` in protected mode; the page calls `notFound()` and the route's existence is invisible to anyone outside `APEX_SOVEREIGN_GITHUB_LOGINS`. Apex source did signin-redirect for auth_required; cerniq's stricter variant drops that because cerniq has no GitHub-OIDC signin flow today (Phase 6 work) and a redirect would only leak the route's existence. In DEMO mode (the default) the gate is a no-op — `requireApexSovereignAccess()` returns null and the page renders mocked data normally, exactly preserving the current Phase 5c behavior. **Page wire-up:** `frontend/app/apex/sovereign/page.tsx` flipped from sync `function ApexSovereignPage()` to `async function ApexSovereignPage()` (legal because the file already declared `export const dynamic = "force-dynamic"`), with a 5-line try/catch at the top of the component: throw `ApexSovereignNotFoundError` → `notFound()`, any other error rethrows. `notFound` imported from `next/navigation`. **Forward compat for sovereign-role unforgeability:** `APEX_SOVEREIGN_GITHUB_LOGINS` is server-only (no `NEXT_PUBLIC_` prefix on purpose — the allowlist must never reach the browser bundle). The role union `ApexRoleSource` keeps the `role_claim`/`group_mapping`/`email_allowlist`/`github_login_allowlist` variants Apex defined even though `deriveApexSession()` only emits `sovereign_github_allowlist`/`default` today — Phase 6 OIDC linking grows new branches without changing the type signature, so absorbed Apex components reading `roleSource` keep compiling. **Test coverage:** `frontend/lib/apex/auth-bridge.test.ts` (~190 lines, 18 specs across 4 describe blocks — `normalizeStringSet`, `resolveSovereignFromGithubLogin`, `isApexProtectedMode`, `deriveApexSession`); `frontend/lib/apex/sovereign-gate.test.ts` (~120 lines, 9 specs across 2 describe blocks — DEMO no-op + protected-mode matrix). Each spec saves/restores `process.env.APEX_SOVEREIGN_GITHUB_LOGINS` + `process.env.NEXT_PUBLIC_DEMO_MODE` via `originalEnv` capture pattern so global env isn't leaked across files. Sovereign-gate spec uses `vi.mock("./auth-bridge", async () => ({ ...await vi.importActual(...), loadApexSessionFromCookies: () => mock() }))` + `await import("./sovereign-gate")` after the mock — the partial-mock keeps `isApexProtectedMode`'s env-read live so DEMO-mode tests are not vacuous. The "name is stable" assertion locks `error.name === "ApexSovereignNotFoundError"` + `error.message === "apex_sovereign_not_found"` for downstream consumers (Sentry breadcrumbs, structured logs) that key on the name string. **Verification scoped to Apex files** (peer 72c0fb06's market-data WIP + 6 pre-existing JSX namespace errors in `app/admin/vendor-status` / `app/alm/market-rates` / `components/risk/YieldCurveCard` continue to block full lint, all outside my path scope): `npx eslint lib/apex app/apex` clean; `npx tsc --noEmit` reports 0 errors in apex/ paths; `npx vitest run lib/apex` — 27/27 specs green in 857ms. **Cross-session protocol:** lane `cerniq:apex-absorption-phase-4` claimed via claude-peers (peers `4dac2c80` vercel-stopgap-railway-direct on `vercel.json` only + `72c0fb06` vendor-tier1-tier2-expansion on `backend-node/src/vendor/*` + `market-data/*` — zero file-scope overlap with my apex paths); `git commit --only` enforces commit-time file-set assertion. **Phase 4 done; Phase 6 (262 API handlers + GitHub-OIDC linking + audit-pipeline reattach for sovereign denial events) and Phase 7 (extend `verify-auth-coverage --strict` + `verify-rule-{4,9,11,12}` to scan the absorbed Apex surface) remain open.** PR #66 (Phases 1-3 + 5) explicitly deferred Phase 4 — this commit closes that deferral. — `frontend/lib/apex/auth-bridge.ts` (NEW), `frontend/lib/apex/auth-bridge.test.ts` (NEW), `frontend/lib/apex/sovereign-gate.ts` (NEW), `frontend/lib/apex/sovereign-gate.test.ts` (NEW), `frontend/app/apex/sovereign/page.tsx` (try/catch + async + 1 import block)
