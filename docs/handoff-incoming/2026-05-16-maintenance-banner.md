- 2026-05-16 — **feat(dashboard): `MaintenanceBanner` — auto-detecting bilingual API-health banner — covers the cold-storage revival window AND remains as permanent defensive infrastructure.** Customer-facing fix for the production gap surfaced earlier this session: `api.cerniq.io` SSL cert mismatch (`*.up.railway.app` wildcard, no cert for the custom domain because Railway project was deleted in cold-storage pivot 2026-05-09); the frontend at `cerniq.io` renders the dashboard as an infinite loading spinner because the API fetch fails and there's no graceful error state. **What changed:** (1) NEW `components/MaintenanceBanner.tsx` — `"use client"` component that probes `${NEXT_PUBLIC_API_URL || NEXT_PUBLIC_NODE_API_URL}/health` on mount and every 30s via `setInterval`, using `fetch` with `mode: 'no-cors'` (don't need backend CORS headers — only care WHETHER the connection completes) + `AbortSignal` timeout at 3s. A thrown error (network unreachable / SSL cert mismatch / DNS failure / timeout) counts as "down"; any HTTP response (opaque) counts as "up". On "down" status, renders a fixed-top amber banner with both English and Spanish strings side-by-side (bilingual standard per `project_cerniq` Maria/Roberto/Jose personas — no language switcher needed when both are visible). Render-null SSR-side and during initial probe — "innocent until proven down" — so healthy loads have zero banner flicker. (2) NEW `components/MaintenanceBanner.test.tsx` — 3 vitest cases: initial null render (probe pending), null render on healthy probe, bilingual banner render on rejected probe. Uses `vi.stubEnv` to set `NEXT_PUBLIC_API_URL` because the env-lookup is LAZY (function-local in the source — necessary for test mocking, no-op in production where Next.js inlines `NEXT_PUBLIC_*` at build time anyway). 3/3 green in 853ms. (3) `app/dashboard/layout.tsx` wire-up — single-line `<MaintenanceBanner />` import + render above `{children}`. Scoped to dashboard routes (which is where API calls happen); marketing pages at `/`, `/pricing`, `/get-started` are SSR-only and don't hit the API, so they don't need the banner. **Self-healing semantics:** the 30s re-probe means the banner disappears automatically the moment backend recovers — NO redeploy needed when the Railway revival completes. The "fix" auto-clears. **Permanent infrastructure beyond this revival:** keep this component after backend comes back; it's the same defensive pattern Coinbase / Plaid / Stripe Dashboard use for status banners. **Observability gap (deferred):** could add `Sentry.captureMessage('api-health-probe-failed', 'warning')` in the catch — the project has `@sentry/nextjs` installed — but adding it now means coupling to Sentry config; trivial follow-up commit. **Pre-existing TS errors NOT touched** (out of scope, peer territory): `app/alm/market-rates/page.tsx:35` and `components/risk/YieldCurveCard.tsx:{49,196}` — namespace JSX + recharts Formatter type. Smoke script flags Frontend TypeScript as WARN (advisory), not blocking the deploy gate. **Deploy pipeline:** this commit will trigger an auto-deploy via the existing Vercel pipeline (project `capexcycle`, prior deploys averaged 2m); banner becomes live the moment Vercel marks the deployment Ready. Then visitors hitting `/dashboard` see the maintenance message instead of infinite spinners. — `frontend/components/MaintenanceBanner.tsx` NEW, `frontend/components/MaintenanceBanner.test.tsx` NEW, `frontend/app/dashboard/layout.tsx`
