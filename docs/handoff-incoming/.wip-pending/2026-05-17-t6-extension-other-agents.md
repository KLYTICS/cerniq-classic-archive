- 2026-05-17 — **T6 extension: LLM script fixtures for cfo-copilot / committee-report / risk-monitor + lint-chain wiring for the T10 endpoint drift guard.** Closes the remaining ALM-decision-only gap from T6 (2026-05-17 closure) so every agent type with a golden case under `backend-node/test/agent-evals/cases/` now has at least one deterministic Anthropic-replay script — full coverage for the 4-agent surface (alm-decision 10/10, cfo-copilot 1/1, committee-report 1/1, risk-monitor 2/2). Also wires the new `verify-agent-smoke-endpoints` script into `npm run lint` so controller-route drift fails CI at lint time. **What landed:** (1) Four new scripts: `cfo-copilot/001.script.ts` (rate-shock +200bps question — 2 tool calls runRateShock + getRepricingGap, 3-turn, output matches CFOCopilotOutputSchema with message ≤ 300 words and exactly 4 EN/ES followups), `committee-report/001.script.ts` (bilingual board report from sourceRunId — 1 tool call runFullSwarm, 2-turn, output matches CommitteeReportOutputSchema with all 8 sections — executiveSummary ≤ 150 words, financialPosition/interestRateRisk/creditConcentration/liquidityRisk/peerComparison narratives, 4 recommendations with owner+deadline+regulatoryRef+expectedImpact, 3 regulatoryCalendar items with status enum, plus pdfPath + bilingualEsPath), `risk-monitor/001.script.ts` (quiet run — 4 tool calls getLCR + getCapitalAdequacy + getCECL + getConcentration, output is `{alerts: [], alertCount: 0, quietRun: true}` per RiskMonitorOutputSchema, locks the "silence is signal" contract — the agent must NOT fabricate alerts), `risk-monitor/002.script.ts` (critical LCR breach — 5 tool calls getLCR + getNSFR + getCapitalAdequacy + getCECL + getConcentration, output has 3 alerts: CRITICAL liquidity at LCR 94% with COSSEC Reg 8866 ref + 2026-06-16 ISO deadline + EN/ES findings + dedupSeed; HIGH NSFR-policy alert; MEDIUM net-worth-watch alert with stress-modeled projection — locks the regulatory-vs-policy escalation contract). (2) New contract spec at `backend-node/src/__tests__/llm-script-contracts-other-agents.spec.ts` (16 tests = 4 invariants × 4 scripts): asserts each script ends with `stopReason: 'end_turn'` (ScriptBuilder contract re-asserted post-import), trace synthesized from tool_use turns is ≥ `minToolsCalled` for the paired golden case, every `requiredTool` from the JSON appears in the trace, and the end_turn payload parses as valid JSON. Lighter than the 006-010 pass-threshold spec because the 6-dimension composite scorer (`scoring/dimensions.ts` + `scoring/weights.ts`) is ALM-decision-shaped — it looks at `output.topRisks[].dollarImpact`, `output.decisionQueue[]`, `output.brief / briefEs` — and would score these other shapes (alerts[], sections.{...}, message+followups) at 0 spuriously. Full pass-threshold lock for non-ALM agent types awaits a scorer generalization (a future T6.b — extract `findings`/`recommendations` from each agent type's native shape via small adapter functions, keep the 6-dimension weights). Spec carries `verify:no-orphan-spec-skip` integration-scope rationale. (3) `backend-node/package.json` updated: new script `"verify:agent-smoke-endpoints": "node scripts/verify-agent-smoke-endpoints.mjs"` added next to `verify:vendor-registry`, and appended to the `lint` chain right after `verify:vendor-registry`. Any future rename of a route in `src/agent-api/agent-runs.controller.ts` — or a bump of the controller prefix from `api/v1/...` — will fail `npm run lint` (which runs in pre-commit + CI) with the exact missing routes named and a 3-step remediation pointer (update script, update REQUIRED_ROUTES, update AGENT_SMOKE.md). **Why this matters for going-live:** before this commit, only alm-decision had deterministic-replay scripts — the agent-eval CI gate could measure regression on 10 of the 33 declared goldens but had nothing for cfo-copilot, committee-report, or risk-monitor. The 4 new scripts close that gap so a future integration suite (`agent-evals.e2e-spec.ts`) can wire MockLlmBridge → AgentRunnerService end-to-end for every agent type that exposes a golden. The lint-chain wiring closes the T10 silent-drift gap — operators no longer discover broken smoke endpoints on first prod run. **Verification:** `jest src/__tests__/llm-script-contracts-other-agents.spec.ts` → 16/16 green in 0.7s. Combined run (006-010 + other-agents + sentry-smoke) → 31/31 green across 3 suites. `npm run verify:agent-smoke-endpoints` → green (live scan, all 4 routes present). `node scripts/verify-no-orphan-spec.mjs` → 0 orphans (429 specs scanned, 2 skipped — both with integration-scope comments). KLYTICS rules 4/9/11/12 → all green. `tsc --noEmit -p tsconfig.json` → no errors. **Coverage matrix:** alm-decision 10/10 scripts, cfo-copilot 1/1, committee-report 1/1, risk-monitor 2/2. **Files:** `backend-node/test/agent-evals/cases/cfo-copilot/001.script.ts`, `.../committee-report/001.script.ts`, `.../risk-monitor/001.script.ts`, `.../risk-monitor/002.script.ts`, `backend-node/src/__tests__/llm-script-contracts-other-agents.spec.ts`, `backend-node/package.json`.
