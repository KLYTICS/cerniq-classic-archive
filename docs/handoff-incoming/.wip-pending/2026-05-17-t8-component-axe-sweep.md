- 2026-05-17 — **T8 closure: component-level axe-core a11y sweep wired into vitest.** Complements (does not replace) the existing Playwright route sweep at `frontend/e2e/a11y-sweep/`. The route sweep needs a live dev server + Playwright workers and so only runs in its dedicated CI job; the new component sweep runs inside `vitest run` and gives every terminal widget its own a11y ratchet independent of which routes mount it. **What landed:** (1) `frontend/lib/test-utils/a11y.ts` — `axeRender()` wrapper that renders a React element with RTL and runs `axe.run()` against the resulting container; throws on any non-empty violation list with rule id, impact, node count, and first three offending selectors per violation. Disables four layout-dependent rules (`color-contrast`, `color-contrast-enhanced`, `target-size`, `scrollable-region-focusable`) because jsdom can't compute real layout — those stay with the Playwright sweep as the source of truth. Default tag set is `wcag2a wcag2aa wcag21a wcag21aa`. Also exports `axeCheck()` for tests that render outside `render()`. (2) 11 new a11y assertions appended to the three terminal-component test files: `components/alm/health-score-widget.test.tsx` (+4: full English, compact, Spanish, high-risk red band), `components/alm/scenario-input.test.tsx` (+3: default English, Spanish locale, custom default values — slider-heavy form is the highest aria-label exposure), `components/agents/alert-feed.test.tsx` (+4: rendered feed, Spanish, empty state, filters-hidden). Tests appended to existing files rather than created as separate `.a11y.test.tsx` files so `verify-no-orphan-tests` rule #1 (co-located pairing) stays clean — orphan-test verifier still reports 0 orphans across 89 scanned files. (3) `follow-up-pills` was named in §4.7 T8 but doesn't exist on disk — confirmed via `grep -r FollowUpPill` in `components/` and `app/`. T8 originally specified 4 components; 3 covered, 1 was speculative. **Why this matters for going-live:** the route-level Playwright sweep already covers `/ai-insights`, `/enterprise`, `/market-data`, `/portal/benchmarks`, `/portal/cpa-dashboard`, `/portal/exam-prep`, and the three pages that host these terminal components (`/alm/agents/alerts`, `/alm/copilot`, `/alm/decisions`) — but its baseline.json is `{}` and ratchet.json has `moderateNodeCount: 0` with `updatedAt: null`, meaning the sweep has never been armed against real measurements. Component-level vitest a11y is the layer that runs on every commit without requiring `next dev` to come up, so structural ARIA/role/label regressions get caught immediately even before the full Playwright sweep runs. **Verification:** `vitest run` against the three changed test files → 72/72 green (29 health-score-widget + 22 alert-feed + 21 scenario-input). `npx eslint` against all four touched files → clean. `tsc --noEmit` against the project → no new errors. `node scripts/verify-no-orphan-tests.mjs` → 0 orphans, 89 files scanned. **Files:** `frontend/lib/test-utils/a11y.ts`, `frontend/components/alm/health-score-widget.test.tsx`, `frontend/components/alm/scenario-input.test.tsx`, `frontend/components/agents/alert-feed.test.tsx`.
