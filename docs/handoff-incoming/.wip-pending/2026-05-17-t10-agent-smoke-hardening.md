- 2026-05-17 — **T10 partial closure: agent-smoke.sh hardening + operator runbook + endpoint drift guard.** The script existed before this session but was named in §4.7 as "needs Erwin ops" and unverified for production safety. This commit makes it ship-gate ready: a pre-flight reachability check that fails fast on wrong URL/down backend, an explicit HTTP-status assertion on the cross-tenant RLS test (distinguishes guard-deny 403/404 from RLS-empties-result 200 + 0 runs vs. the actual leak case of 200 + non-empty runs), runId echoed in the summary with the SQL to delete the smoke artifact, and a dedicated `docs/ops/AGENT_SMOKE.md` operator runbook. **What landed:** (1) `scripts/agent-smoke.sh` improvements: new Step 0 pre-flight (`GET /health` with 10s `--max-time`, exits 2 if unreachable so operators don't waste time on "why does Step 1 give an opaque curl error"), tightened Step 5 to capture HTTP status code separately and split the pass criterion into three clear cases (403/404 guard-deny → pass / 200 + 0 runs → pass with RLS-emptied note / 200 + N runs → fail with explicit "RLS LEAK" message), runId echoed in summary with the cleanup SQL, expanded usage banner naming the four arg slots + exit codes (0/1/2/64). API_URL trailing slash stripped so URL composition is consistent. (2) New `docs/ops/AGENT_SMOKE.md` operator runbook (~140 lines): when to run (after every prod deploy touching agents/, before first paying customer, after key rotation), prerequisites table (API_URL/Redis/ANTHROPIC_API_KEY/migrations/JWT/INST_ID/CROSS_JWT) with curl one-liner for JWT acquisition, per-step pass-criterion table with common failure modes, triage cheat-sheet for each step (Step 2 hangs at QUEUED → worker not consuming queue; Step 4 reports 0 cost → LlmBridge not setting tokens; Step 5 reports LEAK → P0 rollback + audit). (3) New `backend-node/scripts/verify-agent-smoke-endpoints.mjs` static drift guard (~150 lines, no deps): scans `src/agent-api/agent-runs.controller.ts` for `@Controller(...)` + `@Get/@Post/@Put/@Delete/@Patch` decorators, asserts the prefix is exactly `api/v1/agents/:institutionId` AND the four required sub-paths are present (`POST run`, `GET runs/:runId`, `GET runs/:runId/trace`, `GET runs`). If anyone renames or removes one of those routes — or bumps the controller prefix — CI fails loudly with the exact missing route(s) and a three-step remediation pointer (update script, update REQUIRED_ROUTES const, update the AGENT_SMOKE.md table). Embedded `--self-test` runs 5 fixtures (9 assertions) against the parser: all four routes present (clean), renamed trace route (drift detected on the one route), bumped prefix to v2 (all four routes reported missing — the worst silent-failure case), missing @Controller decorator (throws), double-quoted decorators (also parsed). All 9 self-test assertions pass. Live scan against the current controller passes — all 4 required routes present in `api/v1/agents/:institutionId`. **Why this matters for going-live:** the script in its previous form would pass syntax check but break silently on three classes of drift (route rename, prefix bump, route removal) — and operators would only catch it on first prod ship-gate run, wasting a debug cycle. The drift guard moves that detection to CI. The pre-flight check converts the "curl returns an opaque error" failure mode into a clear "API unreachable at $URL/health" message + exit 2. The Step 5 HTTP-status assertion converts the "RLS leak hidden behind ambiguous 0-runs response" failure mode into either "guard denied" or "RLS emptied" or "LEAK — fix immediately, do not ship". **What remains for full T10 closure** (Erwin-side only): run the script against staging/prod with a real JWT + cross-tenant JWT + Redis up + ANTHROPIC_API_KEY set, confirm PASS exit 0, delete the smoke run row via the printed SQL. The doc spells out exactly how to acquire each input. **Verification:** `bash -n scripts/agent-smoke.sh` → syntax clean; missing-args invocation exits 1 with usage message; `node scripts/verify-agent-smoke-endpoints.mjs --self-test` → 9/9 green; live scan → all 4 routes present. Optional: wire `verify-agent-smoke-endpoints.mjs` into `backend-node/package.json` `lint` script — left for the next commit so the lint chain isn't expanded in this single landing. **Files:** `scripts/agent-smoke.sh` (modified), `docs/ops/AGENT_SMOKE.md` (new), `backend-node/scripts/verify-agent-smoke-endpoints.mjs` (new).
