- 2026-05-17 — **T6.b closure: 6-dimension scorer generalized to non-ALM agent types via per-agent adapter + weight registry.** Closes the deferred limitation flagged in the 2026-05-17 T6 extension handoff: cfo-copilot / committee-report / risk-monitor scripts had only a *lighter* contract spec (end_turn + minToolsCalled + requiredTools + valid JSON) because the existing `scoreOffline` was hardcoded to ALM_DECISION's output shape (`topRisks[].dollarImpact`, `decisionQueue[]`, `brief/briefEs`). T6.b adds a per-agent adapter that normalises each agent type's raw output into the common `ScoreableOutput` shape, plus per-agent weight profiles that reflect what each agent type actually optimises for. Result: every LLM-replay-capable agent type now has a full pass-threshold lock at ≥ 80%. **What landed:** (1) New `backend-node/test/agent-evals/scoring/adapters.ts` (~280 LOC): `ScoreableOutput`/`ScoreableTopRisk`/`ScoreableDecisionItem` shared types; `AgentAdapter` type alias; four concrete adapters (`almDecisionAdapter` identity, `riskMonitorAdapter` with quiet-run synthesis + `|delta| × 100_000` sentinel dollar mapping + 50k floor for tiny deltas, `cfoCopilotAdapter` mapping message → brief and followups → decisionQueue with bilingual EN/ES coverage from the schema's bilingual followups, `committeeReportAdapter` mapping 4 risk-domain sections → synthetic topRisks with `$1` sentinel + per-section COSSEC regulatory ref + bilingual mirroring keyed on `bilingualEsPath` presence + calendar items appended as additional topRisks); `adapterFor(agentId)` lookup with case-insensitive normalisation and dash→underscore translation, falling back to ALM identity for unknown agentIds; `weightsFor(agentId)` lookup with four profiles (ALM_DECISION default 25/25/20/15/10/5; RISK_MONITOR 30/15/20/20/10/5 — tool-heavy + reg-driven; CFO_COPILOT 20/0/45/10/20/5 — zeroes dollarQuantification since Q&A is not a risk report, pins specificity at 45% as gating dimension since the 300-word message must contain numeric anchors; COMMITTEE_REPORT 15/15/25/25/15/5 — heavy regulatoryRef for the calendar section). Re-exports `DEFAULT_AGENT_WEIGHTS` so the canonical ALM weights have a stable import path. (2) `test/agent-evals/scoring/weights.ts` updated: `scoreAgentRun()` accepts an optional 5th argument `agentId`. When provided, the adapter for that agentId remaps the raw output before the four shape-dependent scorers (dollar / specificity / regulatoryRef / bilingual) consume it. `formatCompliance` still runs against the raw output (schema validation is on the actual shape, not the adapter shape). When agentId is omitted, the adapter is skipped — identical behaviour to before T6.b, so all existing callers continue working unchanged. (3) `test/agent-evals/runner/eval-runner.ts` updated: `scoreOffline()` now reads `goldenCase.agentId` and passes it to `scoreAgentRun()` along with `weightsFor(goldenCase.agentId)`. (4) **Latent NaN bug fix** in `scoreToolCoverage` (`test/agent-evals/scoring/dimensions.ts`): the prior code treated `expected.requiredTools === []` as truthy and then divided `0 / 0` for `requiredHit`, producing `NaN` that contaminated the composite score. Committee-report case 001 is the first golden with an explicit empty `requiredTools` list and surfaced the bug. Fixed by tightening the guard to `requiredTools && requiredTools.length > 0`. (5) Upgraded `src/__tests__/llm-script-contracts-other-agents.spec.ts`: was 16 contract-only tests; now 20 tests (16 contracts + 4 pass-threshold) — each non-ALM script must score ≥ 0.8 under its own weight profile or the breakdown is printed dimension-by-dimension. (6) New dedicated registry spec at `src/__tests__/agent-eval-adapters.spec.ts` (24 tests): adapter dispatch (identity for ALM_DECISION, case-insensitive lookup, dash normalisation, fallback to identity for unknown agentIds, undefined-input no-crash); RISK_MONITOR adapter (quiet-run synthesizes sentinel topRisk with full coverage; alerts map 1:1 with `dollarImpact = max(|delta| × 100_000, 50_000)`); CFO_COPILOT adapter (empty followups produce empty decisionQueue; followups become bilingual EN/ES decisionQueue entries); COMMITTEE_REPORT adapter (sections without bilingualEsPath leave findingEs empty; with bilingualEsPath mirror EN sections into findingEs; calendar items become additional topRisks); per-agent weight profiles (each sums to 1.0; CFO_COPILOT zeros dollarQuantification; CFO_COPILOT pins specificity at 45%; COMMITTEE_REPORT raises regulatoryRef to 25%; unknown agentId falls back to ALM defaults; `DEFAULT_AGENT_WEIGHTS === DEFAULT_WEIGHTS`); backward-compat (`scoreAgentRun` without agentId scores identically to `scoreAgentRun` with `ALM_DECISION`; empty output still fails — the regression test that locked the original semantics in `eval-runner.spec.ts`). **Why this matters for going-live:** before T6.b, regression-gate.ts could detect drift on alm-decision but not on the other 3 agent types — a regression in cfo-copilot quality would land silently as long as the script contract didn't change. With per-agent scoring, every agent type now has the same kind of CI safety net. The latent NaN bug fix is a separate bonus: any future agent type with an empty `requiredTools` list would have produced phantom failures, which is exactly the silent-failure class CerniQ's invariant suite is designed to prevent. **Coverage:** alm-decision 10 scripts × full pass-threshold ✓, cfo-copilot 1 × full ✓, committee-report 1 × full ✓, risk-monitor 2 × full ✓ — every script in the repo now has a CI-locked pass-threshold. **Verification:** `jest src/__tests__/llm-script-scoring-006-010.spec.ts src/__tests__/llm-script-contracts-other-agents.spec.ts src/__tests__/agent-eval-adapters.spec.ts` → 52/52 green in 0.9s (8 + 20 + 24). `jest --config jest-eval.config.json` (the integration runner suite at `test/agent-evals/`) → 31/31 green. `tsc --noEmit -p tsconfig.json` → no errors. `verify-no-orphan-spec` → 430 specs scanned, 3 skipped (each with integration-scope comment), 0 orphans. KLYTICS rules 4/9/11/12 + verify-agent-smoke-endpoints → all green. **Files:** `backend-node/test/agent-evals/scoring/adapters.ts` (new), `backend-node/test/agent-evals/scoring/weights.ts` (modified — agentId arg + adapter dispatch), `backend-node/test/agent-evals/scoring/dimensions.ts` (modified — NaN fix in scoreToolCoverage), `backend-node/test/agent-evals/runner/eval-runner.ts` (modified — pass agentId through scoreOffline), `backend-node/src/__tests__/llm-script-contracts-other-agents.spec.ts` (upgraded — adds pass-threshold dimension), `backend-node/src/__tests__/agent-eval-adapters.spec.ts` (new).
