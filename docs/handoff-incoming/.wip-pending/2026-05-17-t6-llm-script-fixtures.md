- 2026-05-17 — **T6 closure: LLM script fixtures for ALM-decision goldens 006-010 + pass-threshold lock spec.** Completes the deferred 33-golden replay capability for the alm-decision agent type — together with the pre-existing 001-005 scripts, all 10 alm-decision golden cases now have deterministic Anthropic-replay fixtures ready for `MockLlmBridge → AgentRunnerService` integration. **What landed:** (1) Five new scripts under `backend-node/test/agent-evals/cases/alm-decision/`: `006.script.ts` (deposit flight — 6 tool calls across runFullSwarm/getLCR/getDepositBeta/getNSFR/getConcentration/getCECL, Liquidity Risk topRisk, health 54), `007.script.ts` (EWS composite amber zone — 6 tool calls including runFullSwarm/getEWS/getDepositBeta/getCECL/getRepricingGap/getPeerBenchmark, null topRiskDomain by design — case asserts synthesis-of-weak-signals, health 62, bilingual), `008.script.ts` (balanced healthy pass-case — 7 tool calls, single MEDIUM topRisk (Interest Rate Risk +200bps at 3.9% within policy), only 2 decisionQueue entries because the case explicitly tests "agent does NOT fabricate risks", health 87), `009.script.ts` (hurricane PR Cat-4/5 catastrophic — 8 tool calls hitting all 4 required tools runFullSwarm/runRateShock/getLCR/getCapitalAdequacy plus getCECL/getConcentration/getRepricingGap/runMonteCarlo, 5 CRITICAL/HIGH topRisks with FFIEC BCP regulatory refs, health 28, bilingual), `010.script.ts` (bilingual PR cooperativa — 6 tool calls, Interest Rate Risk topRisk, every brief/finding/action populated EN+ES, COSSEC regulatory refs throughout, health 64). Each follows the canonical builder pattern: `script().forCase(...).addToolUseTurn(...)+ .addEndTurn(JSON.stringify(...))..build()` — same shape as `001.script.ts` through `005.script.ts`. Each output JSON has agentId/version/runId/institutionId/timestamp/language/healthSnapshot/topRisks/decisionQueue/brief/briefEs/auditTraceId. Every topRisk carries dollarImpact > 0 + regulatoryRef + finding + findingEs + toolsUsed. Every decisionQueue entry carries action + actionEs + expectedImpact + regulatoryRef + deadline + owner + status. (2) New pass-threshold lock spec at `backend-node/src/__tests__/llm-script-scoring-006-010.spec.ts` — 8 tests: 5 score-threshold (each script `scoreOffline()` result ≥ 0.8 PASS_THRESHOLD against its paired golden case), + 3 contract locks (every script ends in end_turn per ScriptBuilder; total trace ≥ minToolsCalled; every requiredTool from the JSON appears in the script's tool_use turns). The trace is synthesized from each script's tool_use turns; the output is parsed from the final end_turn JSON. Spec carries `// verify:no-orphan-spec-skip` comment with the integration-scope rationale (no single source module — it tests a bundle of fixtures + their golden cases). **Why this matters for going-live:** before this commit, only 1 of 10 alm-decision goldens (001) had a deterministic replay script; the rest could score offline via `scoreOffline` but couldn't be executed end-to-end through `MockLlmBridge`. With 006-010 landing (after concurrent peer sessions delivered 002-005), the full agent-runner deterministic-replay path is now possible for the entire alm-decision suite — a precondition for the `agent-evals.e2e-spec.ts` integration suite that the doc notes as a future T6.b. cfo-copilot / committee-report / risk-monitor still have 0 scripts each (1+1+2 = 4 goldens un-scripted) — those are the next leverage points if the deterministic-replay coverage is extended beyond alm-decision. **Cleanup note:** two in-session verification scratch files left as neutralized stubs (`backend-node/scripts/_score-verify-006-010.tmp.ts` and `backend-node/test/agent-evals/score-scripts-006-010.tmp.spec.ts`, each 3 lines of `export {}` + a comment naming the persistent equivalent) because the running sandbox couldn't `rm` them through the fuse mount; they can be deleted directly on the host with a one-line `rm`. **Verification:** `jest src/__tests__/llm-script-scoring-006-010.spec.ts` → 8/8 green in 0.6s. Combined run with sentry-smoke + sentry-scrubber specs → 46/46 across 3 suites. `tsc --noEmit -p tsconfig.json` against the whole project → no errors. `verify-no-orphan-spec` → 0 orphans (428 specs scanned). KLYTICS rules 4/9/11/12 → all green. **Score breakdown summary (each ≥ 0.8):** 006 / 007 / 010 hit the 6-tool minimum and bilingual + reg-ref dimensions; 008 deliberately ships 1 topRisk + 2 decisionQueue entries (pass-case requires no fabrication) and still scores ≥ 0.8 because every item carries dollarImpact + reg-ref; 009 hits 8 tools + 5 CRITICAL topRisks with FFIEC BCP refs and bilingual coverage. **Files:** `backend-node/test/agent-evals/cases/alm-decision/006.script.ts`, `.../007.script.ts`, `.../008.script.ts`, `.../009.script.ts`, `.../010.script.ts`, `backend-node/src/__tests__/llm-script-scoring-006-010.spec.ts`.
