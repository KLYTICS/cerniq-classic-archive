name: CI Quick Check
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '20'
  PYTHON_VERSION: '3.12'
  FRONTEND_API_URL: http://localhost:3000

jobs:
  apple-foundation:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - run: bash scripts/apple/verify.sh
      - run: bash scripts/verify-clean-worktree.sh

  apple-release-dry-run:
    if: ${{ secrets.APPLE_DEVELOPMENT_TEAM != '' }}
    runs-on: macos-14
    needs: [apple-foundation]
    steps:
      - uses: actions/checkout@v4
      - run: npm run archive:apple -- --dry-run --export
        env:
          APPLE_DEVELOPMENT_TEAM: ${{ secrets.APPLE_DEVELOPMENT_TEAM }}
      - run: bash scripts/verify-clean-worktree.sh

  backend-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-project
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache-dependency-path: backend-node/package-lock.json
          working-directory: backend-node
      - run: cd backend-node && npx prisma generate
      - run: cd backend-node && npx tsc --noEmit
        env:
          DATABASE_URL: postgresql://<user>@localhost:5432/test

  frontend-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-project
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache-dependency-path: frontend/package-lock.json
          working-directory: frontend
      - run: cd frontend && npx next build
        env:
          NEXT_PUBLIC_API_URL: ${{ env.FRONTEND_API_URL }}

  outbound-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-project
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache-dependency-path: services/outbound/requirements.txt
          working-directory: services/outbound
      - run: cd services/outbound && python -m pytest tests/ -q

  cossec-parser-check:
    # COSSEC PDF parser microservice — 32 pytest tests covering PyMuPDF
    # extraction, Spanish regex for `Hallazgo N°X` headers, CAMEL+S
    # severity mapping, and SHA-256 dedup. Landed in Phase-1.6 commit
    # 601e3525 but had no CI coverage — a regression here would ship to
    # prod undetected until it hit a real COSSEC PDF upload.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-python-project
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache-dependency-path: services/cossec-parser/requirements.txt
          working-directory: services/cossec-parser
      - run: cd services/cossec-parser && python -m pytest tests/ -q

  prisma-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node-project
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache-dependency-path: backend-node/package-lock.json
          working-directory: backend-node
      - run: cd backend-node && npx prisma validate
